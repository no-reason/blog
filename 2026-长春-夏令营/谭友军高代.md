# 谭友军高代

$
\begin{aligned}
&1.Lagrange插值\\
&(待定系数法+Vandermonde行列式)\\
&l_{j}(x)=\frac{\Pi _{1\leq i\leq n}(x-a_{i})}{x-a_{j}}\\
&(考虑上述基)\\
&\\\\
&2.0\ne A_{i}\in M_{n\times n}(F),1\leq i\leq s,证明:\\
&\exists \alpha \in F^{n},s.t. A_{i}\alpha \ne 0,1\leq i\leq s\\
&就是证明若干个真子空间的并不是整个空间\\
&\\\\
&加条件:\\
&dimV_{i}<\infty\\
&就可以有一个新的证明:\\
&引理:\\
&设dim V\geq k\\
&则\exists 无穷多个向量\alpha _1,\cdots ,\alpha _{n}\cdots \\
&s.t. 其中任意k个都是线性无关的\\
&由dim V \geq k可知\\
&\exists \beta _1,\cdots ,\beta _{k}\in V,线性无关\\
&\\
&取c_1,\cdots,\in F互不相同\\
&令\alpha _{i}=(\beta _1,\cdots ,\beta _{k})\begin{bmatrix}
1\\
c_i\\
\vdots \\
c_{i}^{k-1}\\
\end{bmatrix}\in V\\
&\\\\
&1.3(2009南开)\\
&设T是M_{n}(F)上的线性变换、、
&满足对于任意A,B\in M_{n}(F).\\
&有T(AB)=T(A)T(B)与T(AB)=T(B)T(A)中至少有一个成立\\
&\\
&证明:\\
&要么\forall A,B\in M_{n}(F)都有\\
&T(AB)=T(A)T(B)成立\\
&要么\forall A,B\in M_{n}(F)都有\\
&T(AB)=T(B)T(A)成立\\
&\\\\
&考虑u_1=\{A\in M_{n}(F):T(AB)=T(A)T(B),\forall B\in M_{n}(F)均成立\}\\
&u_2=\{A_{n}\in M_{n}(F):T(AB)=T(B)T(A),\forall B\in M_{n}(F)均成立\}\\
&u_1,u_2是M_{n}(F)的子空间\\
&\\\\
&若M_{n}(F)=u_1\cup u_2则原命题成立\\
&\\
&即证:\\
&\forall A\in M_{n}(F)\\
&要证A\in u_1或A\in u_2\\
&考虑V_{A}=\{B\in M_{n}(F):T(AB)=T(A)T(B)\}\\
&\\
&V_{A}'=\{B\in M_{n}(F):T(AB)=T(B)T(A)\}\\
&\Rightarrow V_{A}\cup V_{A}'=M_{n}(F)\\
&故知V_{A}=M_{n}(F)或V_{A}'=M_{n}(F)\\
&又由V_{A}和V_{A}'是子空间\\
&故知原命题成立\\
&\\\\
&(1.4)\forall A\in M_{n}(F)\\
&定义线性变换T_{A}(X)=AX,X\in M_{n}(F)\\
&\forall M_{n}(F)上的线性变换T\\
&是否存在A\in M_{n}(F)s.t. T=T_{A}\\
&\\\\
&考虑\rho :\\
&M_{n}(F)\to End(M_{n}(F))\\
&A\mapsto T_{A}\\
&题目即问:\\
&\rho 是否是满射\\
&\\
&显然\rho 是线性映射\\
&我们只需计算\rho 的ker\\
&\Rightarrow dim ker \rho +dim im (\rho )=dim (M_{n}(F))=n^2\\
&但dim RHS=n^4故知原命题不成立\\
&考虑举反例\\
&T_{A}^2=T_{A^{2}}故以此类推可知\\
&T_{A}的零化多项式的次数一定<n\\
&故知我们取M_{n}上的线性变换T,s.t. T的零化多项式\geq n即可\\
&
&\\\\
&1.5\\
&设A,B\in M_{n}(F).\\
&设矩阵方程XA+BX'=0在M_{n}(F)中有非零解\\
&证明:\exists C\in M_{n}(F),s.t. XA+BX'=C在M_{n}(F)中无解\\
&(Im和Ker简单题或者直接Sylvester矩阵方程算)\\
&\\\\
&1.6(Kac-Moody代数)设A是n阶复方阵\\
&H是复线性空间.\\
&设\exists 线性无关的h_1,\cdots ,h_{n}\in H\\
&和线性无关的f_{1},\cdots ,f_{n}\in H^{*}\\
&s.t. f_{i}(h_{j})=a_{ij}\\
&1\leq i,j\leq n\\
&证明:dim H\geq 2n-r(A)\\
&设dim H=m要证:m\geq 2n-r(A)\\
&(基的扩充定理)\\
&\\\\
&1.7(Dieudonne定理)\\
&(补一下这个题)\\
&\\
&设W=\{\begin{bmatrix}
0\cdots *\\
\vdots\cdots *\\
\end{bmatrix}\in M_{n}(F)\}\\
&则dim W=n^2-n\\
&n=1时显然成立\\
&n>1时\\
&考虑W\cap V\\
&dim W+dim V-dim (W\cap V)=dim (W+V)\\
&\leq n^2\\
&\Rightarrow \\
&dim (W\cap V)\geq (n-1)^{2}\\
&V中有秩为n-1的矩阵\\
&不妨设\begin{bmatrix}
E_{n-1}&0\\
0&0
\end{bmatrix}\\
&考虑空间\\
&U=\{\begin{bmatrix}
0&0\\
0&a\\
\end{bmatrix}a\in F\}\\
&dim(U\cap V)=1或0\\
&若dim (U\cap V)=1则原命题成立\\
&若dim (U\cap V)=0,老师没有做出来\\
&\\\\
&但是我感觉直接更改一下U的取法把a上面的那一列设出自由度来\\
&但是我发现这样也是错的,这样的话可能最后选出来的元素在右下角还是0\\
&\\\\
&1.8(Flanders定理)\\
&设V是M_{n}(F)的子空间\\
&且V中的矩阵的秩最大为m\\
&则dim V\leq mn\\
&\\\\
&设F=R,可以比较容易的证明该结论\\
&W=\begin{bmatrix}
0&A\\
A^{T}&B
\end{bmatrix}\\
&其中A,B为实矩阵\\
&不妨设\begin{bmatrix}
E_{m}&0\\
0&0\\
\end{bmatrix}\in V\\
&\\\\
&(1.9)\\
&(线性不等式组基本定理)\\
&设A=(a_{ij})是m\times n型实矩阵\\
&考虑以A为系数矩阵线性不等式组\\
&\begin{cases}
a_{11}x_1+\cdots +a_{1n}x_{n}>0\\
\vdots\\
a_{n1}x_1+\cdots +a_{nn}x_{n}>0\\
\end{cases}\\
&设r_{i}为A的第i个行向量\\
&证明上述线性不等式组有解\iff \\
&r_{1},\cdots ,r_{n}在负实数集上是线性无关的\\
&\\\\
&1.10\\
&设n\geq 2.设A是有理数域上的n阶方阵\\
&其特征多项式在有理数域上不可约\\
&证明:\\
&对于A的任意特征向量x\\
&其坐标分量在有理数域上线性无关\\
&本题考虑\alpha为行向量 表示的是\alpha x=0\\
&考虑循环子空间(\alpha ,\alpha A^{T},\cdots ,\alpha {A^{T}}^{n-1})即可知原命题得证\\
&\\\\
&(1.11)设V是数域F上的线性空间\\
&V^{*}是V的对偶空间\\
&对V的任意子空间W,设\\
&W^{o}=\{f\in V^{*}:f(\alpha )=0,\forall \alpha \in W\}\\
&\\\\
&证明:\\
&W^{o}是V^{*}的子空间\\
&且,当dim V<\infty时有\\
&dim W+dim W^{o}=dim V\\
&(本题就把基设出来，然后考虑扩充成全空间的基)\\
&(然后再考虑对应的对偶基即可)\\
&\\\\
\end{aligned}
$

### 例1.12
设 $V$ 是数域 $\mathbb{F}$ 上的有限维线性空间，$f$ 是 $V$ 上的一个反对称双线性型。对 $V$ 的任意子空间 $W$ 定义

$$
W^{\perp}=\{\alpha\in V\mid f(\alpha,\beta)=0,\ \forall\,\beta\in W\}.
$$

**(1)** 对 $V$ 的任意子空间 $W$ 证明：
$$
\dim W+\dim W^{\perp}=\dim V+\dim(W\cap V^{\perp}).
$$

**(2)** 设 $V$ 的子空间 $W$ 满足 $W\subset W^{\perp}$，则称 $W$ 为关于 $f$ 是**全迷向的**（totally isotropic）。

(i) 全迷向子空间 $W$ 的维数的最大值为 $\displaystyle\frac12\big(\dim V+\dim V^{\perp}\big)$。

(ii) 设 $W$ 是全迷向子空间。证明：以下四条是等价的：

- (a) $W$ 是极大全迷向的。
- (b) $\displaystyle\dim W=\frac12\big(\dim V+\dim V^{\perp}\big)$。
- (c) $W\supseteq W^{\perp}$。
- (d) $W=W^{\perp}$。



$
\begin{aligned}
&(和radical 的关系)\\
&考虑\rho :W\to V^{*}\\
&\rho (\alpha )(\beta)=f(\alpha ,\beta)\\
&则\rho 是线性的\\
&\Rightarrow \\
&dim W=dim ker \rho +dim Im \rho \\
&\alpha \in ker \rho \iff \alpha \in W^{\perp}\cap V\\
&im \rho =\{\rho (\alpha ),\alpha \in W\}\\
&(im \rho )^{o}=\{r\in (V^{*})^{*},r(im \rho )=0\}\\
&\{r\in V,\rho (\alpha )(r)=0\}\\
&=W^{\perp}\\
&\\\\
&(2)(i)是显然的直接做放缩就可以\\
&这里讨论了一个关于f非退化的一个问题\\
&下面的问题就在于如何构造一个f和W\\
&s.t. dim W=\frac{1}{2}(dim V+dim V^{\perp})\\
&(ii)当W=W^{\perp}的时候\\
&容易证明W^{\perp}\subseteq V^{\perp}\\
&容易证明(ii)\\
&
\end{aligned}
$



通常所谓两类双线性型的“基本定理”，指的是它们在适当基下的标准形分类。以下默认 \(\operatorname{char}F\ne2\)。

对称双线性型满足
\[
f(x,y)=f(y,x).
\]
若其矩阵为 \(A\)，则 \(A^T=A\)。存在一组基，使
\[
A\sim \operatorname{diag}(d_1,\dots,d_r,0,\dots,0),
\qquad d_i\ne0.
\]
这里 \(r=\operatorname{rank}A\)，零块对应根空间
\[
V^\perp=\{x:f(x,V)=0\}.
\]

在 \(\mathbb R\) 上，这进一步成为 Sylvester 惯性定律：
\[
A\sim \operatorname{diag}(I_p,-I_q,0),
\]
其中 \(p,q\) 唯一确定，不依赖所取基。也就是说，对称双线性型由正惯性指数 \(p\)、负惯性指数 \(q\)、零度
\[
\dim V^\perp
\]
完全分类。

在 \(\mathbb C\) 上，每个非零数都有平方根，所以可化为
\[
A\sim \operatorname{diag}(I_r,0),
\]
故只由秩分类。

---

反对称双线性型满足
\[
f(x,y)=-f(y,x).
\]
于是
\[
f(x,x)=0.
\]
其矩阵 \(A\) 满足 \(A^T=-A\)。

基本定理是：存在一组基，使
\[
A\sim
\begin{pmatrix}
J&&&\\
&J&&\\
&&\ddots&\\
&&&J\\
&&&&0
\end{pmatrix},
\qquad
J=
\begin{pmatrix}
0&1\\
-1&0
\end{pmatrix}.
\]
也可写成
\[
A\sim
\begin{pmatrix}
0&I_r&0\\
-I_r&0&0\\
0&0&0
\end{pmatrix}.
\]

因此立刻得到：

\[
\operatorname{rank}A=2r
\]
必为偶数；

\[
\dim V-\dim V^\perp
\]
必为偶数；

若 \(f\) 非退化，即 \(V^\perp=0\)，则
\[
\dim V
\]
必为偶数，并存在一组基
\[
e_1,\dots,e_r,f_1,\dots,f_r
\]
使
\[
f(e_i,e_j)=f(f_i,f_j)=0,\qquad
f(e_i,f_j)=\delta_{ij}.
\]
这称为辛基（symplectic basis），相应的非退化反对称双线性型称为辛型。

---

和你刚才题目的联系是：反对称型的标准形说明
\[
\dim V=\operatorname{rank}f+\dim V^\perp,
\]
且 \(\operatorname{rank}f\) 为偶数。极大全迷向子空间的维数因此是
\[
\frac12\bigl(\dim V+\dim V^\perp\bigr)
=
\frac12\operatorname{rank}f+\dim V^\perp.
\]

当 \(f\) 非退化时，这就退化为最熟悉的结论：
\[
\dim W_{\max}=\frac12\dim V.
\]

特征为 \(2\) 时必须格外小心：\(f(x,y)=-f(y,x)\) 与对称性表面上重合，通常应以额外条件 \(f(x,x)=0\) 来定义“交替双线性型”。

### 补1.6

# 一个关于矩阵秩与空间维数的不等式

## 定理

设 \(H\) 是有限维向量空间。

设：

\[
h_1,\dots,h_n\in H
\]

线性无关；

\[
f_1,\dots,f_n\in H^*
\]

线性无关。

定义矩阵：

\[
A=(a_{ij})\in M_n(F)
\]

其中：

\[
a_{ij}=f_i(h_j).
\]

若：

\[
r(A)=r,
\]

则：

\[
\boxed{\dim H\ge 2n-r}
\]

---

# 证明

设：

\[
m=\dim H.
\]

---

## 1. 构造两个子空间

令：

\[
H_0=\operatorname{span}\{h_1,\dots,h_n\}.
\]

因为：

\[
h_1,\dots,h_n
\]

线性无关，所以：

\[
\dim H_0=n.
\]

---

令：

\[
F_0=\operatorname{span}\{f_1,\dots,f_n\}\subseteq H^*.
\]

因为：

\[
f_1,\dots,f_n
\]

线性无关，所以：

\[
\dim F_0=n.
\]

---

## 2. 定义限制映射

考虑线性映射：

\[
\rho:F_0\rightarrow H_0^*
\]

定义为：

\[
\rho(f)=f|_{H_0}.
\]

即把定义在 \(H\) 上的线性函数限制到子空间 \(H_0\)。

---

取：

\[
f_1,\dots,f_n
\]

作为 \(F_0\) 的基。

取：

\[
h_1,\dots,h_n
\]

作为 \(H_0\) 的基。

则：

\[
\rho(f_i)(h_j)
=
f_i(h_j)
=
a_{ij}.
\]

因此限制映射 \(\rho\) 在上述基下的矩阵就是：

\[
A=(a_{ij}).
\]

故：

\[
\operatorname{rank}\rho=r(A).
\]

---

## 3. 计算核空间维数

由秩零度定理：

\[
\dim\ker\rho
=
\dim F_0-\operatorname{rank}\rho.
\]

因为：

\[
\dim F_0=n,
\]

所以：

\[
\boxed{
\dim\ker\rho=n-r(A)
}
\tag{1}
\]

---

## 4. 估计核空间

若：

\[
f\in\ker\rho,
\]

则：

\[
\rho(f)=0.
\]

即：

\[
f|_{H_0}=0.
\]

因此：

\[
f(h)=0,\qquad\forall h\in H_0.
\]

定义零化子：

\[
H_0^0=
\{g\in H^*:g(h)=0,\forall h\in H_0\}.
\]

于是：

\[
\ker\rho\subseteq H_0^0.
\]

---

## 5. 计算零化子的维数

设：

\[
\dim H=m.
\]

由于：

\[
\dim H_0=n,
\]

所以：

\[
\dim(H/H_0)=m-n.
\]

又因为：

\[
H_0^0\cong(H/H_0)^*,
\]

因此：

\[
\dim H_0^0=m-n.
\]

于是：

\[
\dim\ker\rho\le m-n.
\]

即：

\[
\boxed{
\dim\ker\rho\le m-n
}
\tag{2}
\]

---

## 6. 合并两个估计

由 (1)：

\[
n-r(A)=\dim\ker\rho.
\]

由 (2)：

\[
\dim\ker\rho\le m-n.
\]

因此：

\[
n-r(A)\le m-n.
\]

整理：

\[
m\ge2n-r(A).
\]

即：

\[
\boxed{
\dim H\ge2n-r(A)
}
\]

证毕。

---

# 直观解释

矩阵：

\[
A=(f_i(h_j))
\]

表示线性函数：

\[
f_1,\dots,f_n
\]

作用在向量：

\[
h_1,\dots,h_n
\]

上的信息。

如果：

\[
r(A)<n,
\]

说明这些函数限制到：

\[
H_0=\operatorname{span}\{h_1,\dots,h_n\}
\]

之后出现了线性退化。

退化的维数为：

\[
n-r(A).
\]

但是：

\[
f_1,\dots,f_n
\]

在整个：

\[
H^*
\]

中仍然线性无关。

所以丢失的信息必须由：

\[
H/H_0
\]

补偿。

而：

\[
\dim(H/H_0)=\dim H-n.
\]

因此必须满足：

\[
\dim H-n\ge n-r(A).
\]

最终得到：

\[
\dim H\ge2n-r(A).
\]

---

# 与 Dieudonné 定理的联系

这个证明和 Dieudonné 定理中的核心思想一致：

\[
\boxed{
\text{秩下降}\Longrightarrow\text{额外维数补偿}
}
\]

在 Dieudonné 定理中：

\[
y^Tx=0
\]

限制了两个方向的自由度。

这里：

\[
f_i(h_j)=a_{ij}
\]

的秩亏限制了限制映射的自由度。

二者本质上都是通过：

\[
\text{线性映射的核空间维数}
\]

控制整体维数。

### 例2.1
设 $V$ 是数域 $\mathbb{F}$ 上的线性空间，$S$ 是 $V$ 的非空子集。

**(1)** 在 $V$ 上定义关系 $\sim$ 为：$\alpha \sim \beta$ 当且仅当 $\alpha-\beta\in S$。证明：如果 $S$ 是 $V$ 的子空间，则 $\sim$ 是 $V$ 上的一个等价关系；如果 $\sim$ 是 $V$ 上的一个等价关系，则 $S$ 对 $V$ 的加法封闭。

(能否举一个具体的例子)

**(2)** 设 $S$ 是 $V$ 的子空间。对任意 $\alpha\in V$，记 $\overline{\alpha}=\{\beta\in V\mid \beta\sim\alpha\}$，其中，$\sim$ 是由 (1) 给出的等价关系。（验证：$\overline{\alpha}=\alpha+S$。）记
$$
\overline{V}=\{\,\overline{\alpha}\,\mid \alpha\in V\,\}.
$$
证明：

(i) 对应法则 $(\overline{\alpha_1},\overline{\alpha_2})\mapsto \overline{\alpha_1+\alpha_2}$ 是从 $\overline{V}\times\overline{V}$ 到 $\overline{V}$ 的映射；对应法则 $(k,\overline{\alpha})\mapsto \overline{k\alpha}$ 是从 $\mathbb{F}\times\overline{V}$ 到 $\overline{V}$ 的映射。

(ii) $\overline{V}$ 关于上述两个映射成为 $\mathbb{F}$ 上的一个线性空间，称为 $V$ 的一个**商空间**，记为 $V/S$。

(iii) 映射 $\pi\colon \alpha\mapsto \overline{\alpha}$ 是从 $V$ 到 $V/S$ 的满的线性映射，称为**自然满射**（canonical surjection）。

(iv) $\dim S+\dim V/S=\dim V$。（因此 $S$ 在 $V$ 中的余维数（codimension）$\operatorname{codim} S$ 就是 $\dim V/S$。）


### 例2.2
设 $f\colon V \to W$ 是线性映射。

**(1)** 证明：$V/\ker f \cong \operatorname{im}(f)$，并由此推出线性映射的维数公式。（事实上，任意商空间 $V/S$ 都具有这种形式，即，存在线性映射 $f$ 使得 $V/S = V/\ker f$。）

**(2)** 设 $U$ 是 $V$ 的子空间且 $U \subseteq \ker f$。证明：存在唯一的线性映射 $g\colon V/U \to W$ 使得 $g\pi = f$，其中，$\pi\colon V \to V/U$ 是自然满射。




### 例2.3
设 $U_1, U_2$ 是 $V$ 的子空间且 $U_1 \subseteq U_2$。证明：$U_2/U_1$ 是 $V/U_1$ 的子空间，且
$$
(V/U_1)\big/(U_2/U_1) \cong V/U_2.
$$


### 例2.4
设 $T$ 是线性空间 $V$ 的线性变换，$U$ 是 $T$-子空间。

**(1)** 证明：$\overline{\alpha}\mapsto \overline{T(\alpha)}$ 是 $V/U$ 上的线性变换，称为由 $T$ 诱导的线性变换，记为 $\overline{T}$.

**(2)** 设 $\dim V<\infty$。设 $f(x), f_U(x), f_{V/U}(x)$ 分别是 $T$, $T|_U$ 和 $\overline{T}$ 的特征多项式。证明：$f(x)=f_U(x)f_{V/U}(x)$.


例2.4的证明可以通过依靠分块矩阵来做证明


### 例2.5
设 $V_1,V_2$ 是 $V$ 的子空间。证明：
$$
(V_1+V_2)/V_1 \cong V_2/(V_1\cap V_2),
$$
并由此推出子空间的维数公式。

简单题直接构造映射做完了


