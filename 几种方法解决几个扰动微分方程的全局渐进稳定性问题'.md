# 两种方法解决几个扰动微分方程的全局解渐近行为问题

## 前言

笔者在之前的数学竞赛夏令营期间，遇到了如下形式的微分方程问题：

给定函数 $f(x)$ 在 $[0,+\infty)$ 上可导（有时进一步要求连续可微），且

$$
\lim_{x\to+\infty}P(f(x),f'(x))=0,
$$

证明

$$
\lim_{x\to+\infty}f(x)=0,
$$

其中 $P$ 是关于 $f,f'$ 的一个二元函数。

当然，并不是任意给定的 $P$ 都能推出上述结论。由于笔者水平有限，本文不讨论一般的判定条件，只整理遇到该类问题时可以考虑的两种思考方向：

1. 对于极值点进行分类讨论；
2. 在函数有界情况下利用近极值点引理。

下面以两道题目为例，说明这两种思路的具体用法。
### 例 23

设 $\alpha>1$，函数 $f(x)$ 在 $[0,+\infty)$ 上可导，且

$$
\lim_{x\to+\infty}\left(f'(x)-|f(x)|^\alpha\right)=0.
$$

求证：

$$
\lim_{x\to+\infty}f(x)=0.
$$

### 例 24

设 $f(x)$ 在 $[0,+\infty)$ 上连续可微，且满足

$$
(f'(x))^2+f^3(x)\to0
\qquad(x\to+\infty).
$$

求证：

$$
f(x)\to0
\qquad(x\to+\infty).
$$

## 对极值点进行分类讨论

### 方法说明

在该方法中，我们首先讨论：**是否存在一列趋于无穷的驻点**，即是否存在

$$
x_n\uparrow+\infty,\qquad f'(x_n)=0.
$$


**第一种情况：存在这样的驻点列。**

在每个区间 $[x_n,x_{n+1}]$ 上，由连续性，$f$ 可以取到最大值和最小值。分别设取值点为 $t_n,s_n$，则

$$
f(s_n)\le f(x)\le f(t_n),\qquad x\in[x_n,x_{n+1}].
$$

若取值点在区间内部，由费马定理，其导数为零；若取值点在端点，端点本来就是驻点。因此总有

$$
f'(t_n)=f'(s_n)=0.
$$

对于本文的两道题，将这些点代入题设，均可推出

$$
f(t_n)\to0,\qquad f(s_n)\to0.
$$

再由夹逼定理，即得 $f(x)\to0$。

**第二种情况：不存在这样的驻点列。**

此时驻点集合有界，故存在 $A>0$，使得

$$
f'(x)\ne0,\qquad x>A.
$$

导数具有介值性（达布定理），所以即使 $f'$ 不连续，也不能在不取零值的情况下改变符号。因此在 $(A,+\infty)$ 上，要么恒有 $f'>0$，要么恒有 $f'<0$。

于是 $f$ 最终严格单调，存在扩展实数意义下的极限

$$
L=\lim_{x\to+\infty}f(x)\in[-\infty,+\infty].
$$

接下来结合题目，排除 $L\ne0$ 的可能性。

### 例 23 的解法一

记

$$
r(x)=f'(x)-|f(x)|^\alpha\to0,
$$

即

$$
f'(x)=|f(x)|^\alpha+r(x).
$$

**（1）存在趋于无穷的驻点列。**

按上面的记号，在最大值点与最小值点处有

$$
|f(t_n)|^\alpha=-r(t_n)\to0,
\qquad
|f(s_n)|^\alpha=-r(s_n)\to0.
$$

所以 $f(t_n),f(s_n)\to0$，由夹逼定理得 $f(x)\to0$。

**（2）$f$ 最终严格单调。**

设其极限为 $L\in[-\infty,+\infty]$，分三步讨论。

若 $L\in\mathbb R$ 且 $L\ne0$，则

$$
f'(x)=|f(x)|^\alpha+r(x)\to|L|^\alpha>0.
$$

故存在 $c>0$，使得充分大的 $x$ 满足 $f'(x)\ge c$。由拉格朗日中值定理，固定充分大的 $B$，有

$$
f(x)\ge f(B)+c(x-B),\qquad x>B,
$$

与 $f(x)\to L\in\mathbb R$ 矛盾。因此有限极限只能是零。

若 $L=-\infty$，则由 $r(x)\to0$ 可知，充分大的 $x$ 满足

$$
f'(x)=|f(x)|^\alpha+r(x)>0.
$$

这意味着 $f$ 最终递增，从而有一个有限的下界，不可能趋于 $-\infty$。

若 $L=+\infty$，则可取 $B$ 足够大，使得对所有 $x\ge B$，

$$
f(x)>0,\qquad f'(x)\ge\frac12 f(x)^\alpha.
$$

因为 $\alpha>1$，所以

$$
\left(f(x)^{1-\alpha}\right)'
=(1-\alpha)f(x)^{-\alpha}f'(x)
\le-\frac{\alpha-1}{2}.
$$

再由中值定理，

$$
0<f(x)^{1-\alpha}
\le f(B)^{1-\alpha}-\frac{\alpha-1}{2}(x-B).
$$

当 $x$ 足够大时，右端为负，矛盾。这里正是利用了 $f$ 在整个半轴上都有定义这一条件。

综上，唯一可能是

$$
\boxed{\lim_{x\to+\infty}f(x)=0.}
$$

### 例 24 的解法一

记

$$
r(x)=(f'(x))^2+f(x)^3\to0,
$$

于是

$$
(f'(x))^2=r(x)-f(x)^3.
$$

**（1）存在趋于无穷的驻点列。**

在相应的最大值点与最小值点处，

$$
f(t_n)^3=r(t_n)\to0,
\qquad
f(s_n)^3=r(s_n)\to0.
$$

因此 $f(t_n),f(s_n)\to0$，夹逼可得 $f(x)\to0$。

**（2）$f$ 最终严格单调。**

首先，由 $(f')^2\ge0$，

$$
f(x)^3\le r(x).
$$

由于实数上的立方根函数递增且连续，有

$$
f(x)\le\sqrt[3]{r(x)},
\qquad
\limsup_{x\to+\infty}f(x)\le0.
$$

所以单调极限 $L$ 只能属于 $[-\infty,0]$。

若 $-\infty<L<0$，则

$$
(f'(x))^2\to-L^3>0.
$$

从而存在 $c>0$，使得充分大的 $x$ 满足 $|f'(x)|\ge c$。又因为 $f'$ 最终符号固定，所以要么恒有 $f'\ge c$，要么恒有 $f'\le-c$。由中值定理，前者迫使 $f$ 趋于 $+\infty$，后者迫使 $f$ 趋于 $-\infty$，均与有限极限矛盾。

若 $L=-\infty$，则 $f$ 最终严格递减。令

$$
u(x)=-f(x),
$$

则充分大的 $x$ 满足 $u(x)>0$、$u'(x)>0$ 且 $u(x)\to+\infty$。题设变为

$$
(u'(x))^2=u(x)^3+r(x).
$$

取 $B$ 足够大，便有

$$
u'(x)\ge\frac1{\sqrt2}u(x)^{3/2},\qquad x\ge B.
$$

于是

$$
\left(u(x)^{-1/2}\right)'
=-\frac12u(x)^{-3/2}u'(x)
\le-\frac1{2\sqrt2}.
$$

故

$$
0<u(x)^{-1/2}
\le u(B)^{-1/2}-\frac{x-B}{2\sqrt2},
$$

右端最终为负，矛盾。

因此只能有 $L=0$，即

$$
\boxed{\lim_{x\to+\infty}f(x)=0.}
$$

## 在函数有界情况下利用近极值点引理

### 近极值点引理

设 $f$ 在 $[0,+\infty)$ 上可导。

若

$$
\limsup_{x\to+\infty}f(x)=M\in\mathbb R,
$$

则存在 $x_n\to+\infty$，使得

$$
f(x_n)\to M,\qquad f'(x_n)\to0.
$$

同理，若

$$
\liminf_{x\to+\infty}f(x)=m\in\mathbb R,
$$

则存在 $y_n\to+\infty$，使得

$$
f(y_n)\to m,\qquad f'(y_n)\to0.
$$

这里的 $M,m$ 必须是有限实数。对于有界函数，两者自然满足这一要求。另外，$M,m$ 分别是上极限、下极限，并非整个定义域上的上确界、下确界。

### 引理之证明

我们只证明上极限的情况，下极限的情况对 $-f$ 应用结论即可。

设

$$
S_n=\sup_{x\ge n}f(x).
$$

由于 $M$ 有限，且 $f$ 在有限闭区间上连续，$S_n$ 有限，并且

$$
S_n\downarrow M.
$$

对每个正整数 $n$，选取 $a_n\ge n+1$，使得

$$
f(a_n)>S_{n+1}-\frac1{n^2}.
$$

令

$$
d_n=S_n-S_{n+1}+\frac1{n^2}>0.
$$

则 $d_n\to0$，且

$$
0\le S_n-f(a_n)<d_n.
$$

在 $[n,+\infty)$ 上定义

$$
g_n(y)=f(y)-2d_n(y-a_n)^2.
$$

由于 $f(y)\le S_n$，故 $g_n(y)\to-\infty$（$y\to+\infty$）。结合连续性，$g_n$ 在 $[n,+\infty)$ 上能取到最大值，设最大值点为 $b_n$。

又因为 $a_n-n\ge1$，

$$
g_n(n)\le S_n-2d_n<f(a_n)=g_n(a_n),
$$

所以 $b_n\ne n$。因此 $b_n$ 是定义区间的内点，可以使用费马定理：

$$
g_n'(b_n)=0,
\qquad
f'(b_n)=4d_n(b_n-a_n).
$$

另一方面，由 $g_n(b_n)\ge g_n(a_n)$，

$$
2d_n(b_n-a_n)^2
\le f(b_n)-f(a_n)
\le S_n-f(a_n)<d_n.
$$

所以

$$
|b_n-a_n|<\frac1{\sqrt2},
\qquad
|f'(b_n)|\le2\sqrt2\,d_n\to0.
$$

同时 $b_n\ge n\to+\infty$，且

$$
S_{n+1}-\frac1{n^2}<f(a_n)\le f(b_n)\le S_n.
$$

由夹逼定理，$f(b_n)\to M$。将 $b_n$ 重命名为 $x_n$，就得到所需结论。证毕。


### 例 23 的解法二

记

$$
r(x)=f'(x)-|f(x)|^\alpha\to0.
$$

**第一步：证明 $f$ 有界。**

取 $A>0$，使得 $x\ge A$ 时 $|r(x)|\le1$。再取 $R>0$，使得 $R^\alpha\ge2$。于是只要 $x\ge A$ 且 $|f(x)|\ge R$，就有

$$
f'(x)\ge |f(x)|^\alpha-1
\ge\frac12|f(x)|^\alpha>0.
$$

先证下界。取

$$
B<\min\{f(A),-R\}.
$$

若 $f$ 在 $A$ 之后能达到 $B$，记第一次达到该值的时刻为 $\tau$。在 $\tau$ 左侧有 $f(x)>B=f(\tau)$，故由左差商可得 $f'(\tau)\le0$。但 $|f(\tau)|>R$，上述估计又给出 $f'(\tau)>0$，矛盾。因此 $f(x)>B$ 对所有 $x\ge A$ 成立。

再证上界。若存在 $x_0\ge A$ 使 $f(x_0)>R$，则只要 $f>R$，就有 $f'>0$，因而 $f$ 不可能在以后回落到 $R$。严格地说，若存在第一次回到 $R$ 的时刻，则此前 $f$ 严格递增，与回到更小值矛盾。

所以对所有 $x\ge x_0$，

$$
f(x)>R,\qquad f'(x)\ge\frac12f(x)^\alpha.
$$

于是

$$
0<f(x)^{1-\alpha}
\le f(x_0)^{1-\alpha}-\frac{\alpha-1}{2}(x-x_0),
$$

右端最终为负，矛盾。因此 $x\ge A$ 时 $f(x)\le R$。

结合 $f$ 在 $[0,A]$ 上连续，得到 $f$ 在整个半轴上有界。

**第二步：应用近极值点引理。**

设

$$
M=\limsup_{x\to+\infty}f(x),
\qquad
m=\liminf_{x\to+\infty}f(x).
$$

由有界性，$M,m\in\mathbb R$。由引理可取 $x_n,y_n\to+\infty$，使得

$$
\begin{aligned}
&f(x_n)\to M,\qquad f'(x_n)\to0,\\
&f(y_n)\to m,\qquad f'(y_n)\to0.
\end{aligned}
$$

代入题设关系，分别得到

$$
|M|^\alpha=0,\qquad |m|^\alpha=0.
$$

故 $M=m=0$，于是

$$
\boxed{\lim_{x\to+\infty}f(x)=0.}
$$

### 例 24 的解法二

记

$$
r(x)=(f'(x))^2+f(x)^3\to0.
$$

**第一步：证明 $f$ 有界。**

取 $A>0$，使得 $x\ge A$ 时 $|r(x)|\le1$。由于

$$
f(x)^3\le r(x)\le1,
$$

可知 $f(x)\le1$，故上界已经得到。

下面证明下界。取 $R>0$，使得 $R^3\ge2$。当 $x\ge A$ 且 $f(x)\le-R$ 时，

$$
(f'(x))^2=(-f(x))^3+r(x)
\ge\frac12(-f(x))^3>0.
$$

因此在 $f\le-R$ 的区域内，导数不能为零。

取

$$
B<\min\{f(A),-R\}.
$$

假设 $f$ 在 $A$ 之后能够达到 $B$，记第一次达到的时刻为 $\tau$。由左差商，$f'(\tau)\le0$；而上述估计说明 $f'(\tau)\ne0$，故 $f'(\tau)<0$。

由于 $f'$ 连续，在 $f<-R$ 的同一个连通区间内，$f'$ 不能改变符号。因此从 $\tau$ 开始，$f$ 持续递减，也就不可能向上回到 $-R$。这一状态会持续到任意大的 $x$：若该区间有有限右端点，连续性与递减性将使端点处仍有 $f\le B<-R$，与其为右端点矛盾。

令 $u=-f$，便在 $[\tau,+\infty)$ 上有

$$
u>0,\qquad u'=-f'>0,
\qquad u'\ge\frac1{\sqrt2}u^{3/2}.
$$

于是

$$
0<u(x)^{-1/2}
\le u(\tau)^{-1/2}-\frac{x-\tau}{2\sqrt2},
$$

右端最终为负，矛盾。因此 $f$ 不可能达到 $B$，从而在 $[A,+\infty)$ 上有下界。

结合有限闭区间上的连续性，$f$ 在整个半轴上有界。

**第二步：应用近极值点引理。**

设

$$
M=\limsup_{x\to+\infty}f(x),
\qquad
m=\liminf_{x\to+\infty}f(x).
$$

由引理，存在 $x_n,y_n\to+\infty$，满足

$$
\begin{aligned}
&f(x_n)\to M,\qquad f'(x_n)\to0,\\
&f(y_n)\to m,\qquad f'(y_n)\to0.
\end{aligned}
$$

分别将两列点代入

$$
(f'(x))^2+f(x)^3=r(x),
$$

令 $n\to+\infty$，得到

$$
M^3=0,\qquad m^3=0.
$$

所以 $M=m=0$，即

$$
\boxed{\lim_{x\to+\infty}f(x)=0.}
$$

## 两种方法的联系

第一种方法使用真正的驻点：如果驻点出现在任意远处，就用相邻所选驻点之间的最大值、最小值夹逼；否则，函数最终单调，转而讨论极限。

第二种方法使用近极值点：先证明有界，再选出函数值接近上、下极限且导数趋于零的点列，把微分关系化为关于极限值的代数关系。

对本文的两道题，这一步分别给出

$$
|M|^\alpha=|m|^\alpha=0,
\qquad\text{或}\qquad
M^3=m^3=0.
$$

两种方法都要处理无穷大的可能性。其共同工具是：若某个正函数 $u$ 在整个尾部满足

$$
u'\ge c u^p,\qquad c>0,\quad p>1,
$$

则

$$
(u^{1-p})'\le-(p-1)c,
$$

这会迫使正量 $u^{1-p}$ 在有限距离后变为非正，与函数在整个半轴上存在矛盾。例 23 中的 $\alpha>1$，以及例 24 中出现的指数 $3/2>1$，都在这里发挥作用。

特别地，例 23 的条件 $\alpha>1$ 不能直接去掉：当 $\alpha=1$ 时，$f(x)=e^x$ 满足 $f'-|f|=0$，却不趋于零。
