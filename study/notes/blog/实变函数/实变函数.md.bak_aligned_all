# 实变函数
## 课程内容概览
- 集合论基础
- 测度论
- 可测函数
- 积分的极限定理
- 微分与积分
- $L^p$ 空间

## 第一章 集合论基础

### 1.1 集合的基本概念
- 集合的元素满足无序性、互异性、确定性。
- 元素关系：$x\in A$ 或 $x\notin A$。
- 罗素悖论提示我们需要公理化集合论。

### 1.2 集合运算
$$
A\cup B=\{x\mid x\in A\ \text{或}\ x\in B\},\quad
A\cap B=\{x\mid x\in A\ \text{且}\ x\in B\}.
$$
$$
A\setminus B=\{x\mid x\in A,\ x\notin B\},\quad A^c=U\setminus A.
$$

### 1.3 德摩根定律
$$
(A\cup B)^c=A^c\cap B^c,\qquad (A\cap B)^c=A^c\cup B^c.
$$
对集合族 $\{A_i\}_{i\in I}$：
$$
\left(\bigcup_{i\in I}A_i\right)^c=\bigcap_{i\in I}A_i^c,
\qquad
$$
### 1.4 集合列的上下极限
$$
\limsup_{n\to\infty}A_n=\bigcap_{n=1}^{\infty}\bigcup_{k=n}^{\infty}A_k,
\qquad
\liminf_{n\to\infty}A_n=\bigcup_{n=1}^{\infty}\bigcap_{k=n}^{\infty}A_k.
$$
- $\limsup A_n$：属于无穷多个 $A_n$。
- $\liminf A_n$：属于几乎所有 $A_n$。
- 收敛条件：$\limsup A_n=\liminf A_n$。


### 1.6 可数与不可数
- $\mathbb{N},\mathbb{Z},\mathbb{Q}$ 可数。
- $(0,1)$ 不可数（对角线法）。
- 连续统基数记为 $c=|(0,1)|=|\mathbb{R}|$。
- 关键关系：$2^{\aleph_0}=c$。

### 1.7 势、$\aleph_0$、$c$ 与进位制表示

#### 1.7.1 势与等势
- 若集合 $A,B$ 间存在双射，则称 $A,B$ 等势，记作 $|A|=|B|$。
- 若存在单射 $A\to B$，记作 $|A|\le |B|$。
#### 1.7.2 可数基数 $\aleph_0$
- 与 $\mathbb{N}$ 等势的集合称为可数无限集。
$$
\aleph_0:=|\mathbb{N}|.
$$
- 典型结论：$|\mathbb{Z}|=|\mathbb{Q}|=\aleph_0$。

#### 1.7.3 连续统基数 $c$
- 定义
$$
c:=|(0,1)|=|\mathbb{R}|.
$$
- 由 Cantor 对角线法知 $(0,1)$ 不可数，因此 $c>\aleph_0$。
- 常用等势：$|(0,1)|=|[0,1]|=|\mathbb{R}|$。

#### 1.7.4 进位制与序列表示
- 二进制序列集 $\{0,1\}^{\mathbb{N}}$ 的势记为 $2^{\aleph_0}$。
- 用映射
$$
\Phi:\{0,1\}^{\mathbb{N}}\to[0,1],\qquad
\Phi((b_k))=\sum_{k=1}^{\infty}\frac{b_k}{2^k}.
$$
- 该映射除了二进制不唯一点（如 $0.01111\cdots=0.10000\cdots$）外基本一一对应。
- 不唯一表示点只构成一个可数集（对应有限二进制小数），不会改变势。
- 因而可以得到
$$
|\{0,1\}^{\mathbb{N}}|=|[0,1]|=c,
$$
所以
$$
2^{\aleph_0}=c.
$$

#### 1.7.5 证明链条（建议记忆）
1. $|\mathbb{N}|=\aleph_0$，故 $|\mathcal P(\mathbb{N})|=2^{\aleph_0}$。
2. 用特征函数对应可得
$$
|\mathcal P(\mathbb{N})|=|\{0,1\}^{\mathbb{N}}|.
$$
3. 二进制序列与 $[0,1]$ 等势（忽略可数个二义点）。
4. 得到
$$
2^{\aleph_0}=|[0,1]|=c.
$$

#### 1.7.6 常用等势结论
$$
|(0,1)|=|[0,1]|=|\mathbb{R}|=|\mathbb{R}^n|=c\quad(n\ge 1).
$$
- 另外，$|\mathbb{N}^{\mathbb{N}}|=c$ 也是常用结论。

## 练习题

### 题：设 $A_n=(2,4+(-1)^n)$，求 $\limsup A_n,\liminf A_n$
奇数项 $A_n=(2,3)$，偶数项 $A_n=(2,5)$，故
$$
\limsup A_n=(2,5),\qquad \liminf A_n=(2,3).
$$

## $\mathbb{R}^n$ 中的点集

$$
\begin{aligned}
&\text{距离公理:}\\
&(1)\ d(x,y)\ge 0,\\
&(2)\ d(x,y)=d(y,x),\\
&(3)\ d(x,y)\le d(x,z)+d(z,y).\\
&\text{Cauchy 不等式:}\\
&\left(\sum_{i=1}^{n}\xi_i\eta_i\right)^2
\le
\left(\sum_{i=1}^{n}\xi_i^2\right)\left(\sum_{i=1}^{n}\eta_i^2\right).\\
&\operatorname{diam}(E)=\sup_{x,y\in E}d(x,y).
\end{aligned}
$$

## 开集和闭集

### 导集、闭包、闭集
- 导集：$E'$ 为 $E$ 的聚点集合。
- 闭包：$\overline{E}=E\cup E'$。
- 闭集判别：$E$ 闭 $\iff E'\subset E$。

### 开集定义
$$
G\text{ 开}
\iff
\forall x_0\in G,\ \exists\delta>0,\ B(x_0,\delta)\subset G.
$$
且有：有限交开，任意并开。

### 点的分类
- 内点：$\exists\delta>0$，$B(x_0,\delta)\subset E$。
- 孤立点：$\exists\delta>0$，$B(x_0,\delta)\cap E=\{x_0\}$。
- 边界点：任意邻域同时与 $E$ 和 $E^c$ 相交。

### Cantor 闭区间套定理
若 $F_n$ 非空有界闭，且
$$
F_1\supseteq F_2\supseteq\cdots,
$$
则
$$
\bigcap_{n=1}^{\infty}F_n\ne\varnothing.
$$

### Lindelof 与 Heine-Borel
- Lindelof（$\mathbb{R}^n$）：任意开覆盖有至多可数子覆盖。
- Heine-Borel（$\mathbb{R}^n$）：有界闭集的任意开覆盖有有限子覆盖。

反例：
- 去掉“有界”：$\mathbb{R}=\bigcup_{n\in\mathbb{N}}(-n,n)$，无有限子覆盖。
- 去掉“闭”：$(0,1)=\bigcup_{n\in\mathbb{N}}(\tfrac1n,1)$，无有限子覆盖。

### $\mathbb{R}$ 中开集结构
若 $G\subset\mathbb{R}$ 开，则
$$
G=\bigcup_{i=1}^{\infty}I_i,
$$


$$G_{\delta}集合F_{\sigma }集$$

$$G_{\delta}集表示可数个开集的交集,F_{\sigma}表示可数个闭集的并集$$

## Sigma-代数
$\sigma -代数(\sigma域),设\Gamma是集族,若\forall A\in \Sigma ,若集族\Sigma满足:\\
\emptyset \in \Sigma \\
A\in \Sigma \implies A^c \in \Sigma \\
A_i\in \Sigma \implies \bigcup_{i=1}^{\infty}A_i\in \Sigma \\
则称\Sigma是一个\sigma -代数\\
e.g. \Sigma=\{\emptyset,x\}\\
对于一个集族\Sigma,\\
\exists F\supseteq \Sigma\\
s.t. F是\sigma -代数\\
\\\\
记\Gamma (\Sigma)=\cap _{\alpha \in I}\{F_{\alpha}\supseteq \Sigma:F_{\alpha }是\sigma -代数\}\\
证明:\emptyset\in \Gamma (\Sigma )显然成立\\
若A\in \Gamma (\Sigma )\\
则A\in \cap _{\alpha \in I}\{F_{\alpha}\supseteq \Sigma:F_{\alpha }是\sigma -代数\}\\
\therefore A\in F_{\alpha }\\
\therefore A^c\in F_{\alpha}\\
则\Gamma (\Sigma)一定是\Sigma -代数\\
\\
则上面构造出的代数就称为Borel代数\\$


### Cantor三分集

设
$$
C_0=[0,1].
$$
从 $C_0$ 开始，每一步把每个闭区间的开中间三分之一删去：

- 第一步：删去 $(\tfrac13,\tfrac23)$，得
$$
C_1=[0,\tfrac13]\cup[\tfrac23,1].
$$
- 第二步：再从上面两个区间各删去中间三分之一，得
$$
C_2=[0,\tfrac19]\cup[\tfrac29,\tfrac13]\cup[\tfrac23,\tfrac79]\cup[\tfrac89,1].
$$
- 依次进行，得到递减闭集列
$$
C_0\supset C_1\supset C_2\supset\cdots
$$

定义 Cantor 三分集为
$$
C=\bigcap_{n=0}^{\infty}C_n.
$$

### 等价刻画
对 $x\in[0,1]$，以下描述等价：

1. $x\in C$。
2. $x$ 的三进制展开可写成
$$
x=0.a_1a_2a_3\cdots\ (3),\quad a_k\in\{0,2\}\ (\forall k).
$$
3. $x$ 不会在有限步构造中被删去。

注：三进制表示不唯一点（如 $0.1=0.02222\cdots$）不影响结论。

### 基本性质

1. $C$ 是闭集：因为 $C$ 是闭集列的可数交。
2. $C$ 无内点、无处稠密：每一步都删去中间开区间，任意小区间内都可找到被删去部分。
3. $C$ 是完全集（闭且无孤立点）。
4. $C$ 不可数：与二进制序列集等势，可构造双射
$$
(a_k)_{k\ge1},\ a_k\in\{0,2\}
\longleftrightarrow
(b_k)_{k\ge1},\ b_k\in\{0,1\},\ b_k=a_k/2.
$$
5. $m(C)=0$（Lebesgue 测度为零）：第 $n$ 步剩余区间总长度
$$
\left(\frac{2}{3}\right)^n\to0,
$$
故极限集测度为 $0$。

### 常见结论

- $C$ 是一个典型的“不可数但零测度”集合。
- Cantor 函数在 $C$ 上发生“增长”，在 $[0,1]\setminus C$ 的每个删去区间上常值。


### C的四条重要性质
1.$C是可数开集并的余集,因此C是闭集$

2.$C'\supseteq C,这是说C是自密集$

3.$C中无内点$

4.$C$ 的势为 $c$：$C$ 与 $\{0,2\}^{\mathbb{N}}$ 等势，而 $\{0,2\}^{\mathbb{N}}$ 与 $\{0,1\}^{\mathbb{N}}$ 等势，故 $|C|=c$。

自密且闭的集合称为完备集。

若 $E$ 中没有内点，则称 $E$ 为无处稠密集。

若
$$
X=\bigcup_{i=1}^{\infty}E_i,\quad E_i\text{ 无处稠密},
$$
则称 $X$ 为第一纲集（meagre set）。若X非第一纲，则称X是第二纲。


点和集合间的距离

$d(x_0,F)=\inf_{y\in F}\{d(x_0,y)\}\\
若x_0\in \overline{F},则d(x_0,F)=0\\$$

命题：F是闭集,$$\exists y\in F,s.t. d(x_0,F)=(x_0-y)=d(x_0,y)\\
证明:\forall n,\exists x_n\in F\\
d(x_0,x_n)<\frac{1}{n}+d(x_0,F)\\
又由\{x_n\}有界,\exists 聚点y\\
s.t. (x_{n_k}\to y)\\
d(x_0,y)\leq d(x_0,x_{n_k})+d(x_{n_k},y)\\
其实已经证完了\\
$
# 3.25 轩轩笔记

## 12. 距离函数的连续性证明
**命题：** 设 $f(x) = d(x, F)$，证明 $f(x)$ 在 $\mathbb{R}^n$ 上一致连续。

**证明：**
1. 对于 $\forall \epsilon > 0$ 及 $\forall x, y \in \mathbb{R}^n$，根据距离定义，存在 $z \in F$ 使得 $d(y, z) < d(y, F) + \epsilon$。
2. 根据三角不等式：
   $$d(x, F) \leq d(x, z) \leq d(x, y) + d(y, z)$$
3. 代入步骤 1 的不等式：
   $$d(x, F) < d(x, y) + d(y, F) + \epsilon$$
4. 移项得：$d(x, F) - d(y, F) < d(x, y) + \epsilon$。
5. 由于 $\epsilon$ 的任意性，有 $f(x) - f(y) \leq d(x, y)$。
6. 同理可证 $f(y) - f(x) \leq d(x, y)$。
7. 因此 $|f(x) - f(y)| \leq d(x, y)$，即 $f(x)$ 满足 **Lipschitz 条件**，必一致连续。

---

## 13. Baire 纲定理 (Baire Category Theorem)
**定理描述：** 若 $E \subseteq \mathbb{R}^n$ 且 $E$ 为完备集，若 $E = \bigcup_{k=1}^{\infty} F_k$（其中 $F_k$ 为闭集），且 $E$ 无内点，则必有某个 $F_k$ 含有内点。

**证明要点（反证法）：**
1. 假设所有 $F_k$ 均无内点。
2. 取开球 $B(x_0, \delta_0) \subset E$。由于 $F_1$ 无内点，存在 $x_1$ 和 $\delta_1$ 使得 $\overline{B}(x_1, \delta_1) \subset B(x_0, \delta_0)$ 且 $\overline{B}(x_1, \delta_1) \cap F_1 = \emptyset$。
3. 递归构造：由于 $F_k$ 无内点，可找到一系列闭球 $\overline{B}(x_k, \delta_k)$ 满足：
   - $\overline{B}(x_k, \delta_k) \subset B(x_{k-1}, \delta_{k-1})$
   - $\overline{B}(x_k, \delta_k) \cap F_k = \emptyset$
   - $\delta_k \to 0$
4. 根据**闭集套定理**，$\bigcap_{k=1}^{\infty} \overline{B}(x_k, \delta_k)$ 非空，设其中点为 $y$。
5. 由构造可知 $y \notin F_k$ 对于所有 $k$ 成立，故 $y \notin \bigcup_{k=1}^{\infty} F_k$。
6. 这与 $E = \bigcup F_k$ 矛盾。

---

## 14. 关于有理数集 $\mathbb{Q}$ 的性质
**结论 1：** $\mathbb{Q}$ 不是 $G_{\delta}$ 集。
- **证明思路：** - 已知 $\mathbb{Q}$ 是可列个单点集的并（$F_{\sigma}$ 集）。
    - 若 $\mathbb{Q}$ 是 $G_{\delta}$ 集，则其补集（无理数集）将是 $F_{\sigma}$ 集。
    - 根据 Baire 纲定理，在完备空间 $\mathbb{R}$ 中，若 $\mathbb{R}$ 是可列个闭集的并，则至少有一个闭集含内点。
    - 但 $\mathbb{Q}$ 和无理数集在 $\mathbb{R}$ 中均稠密且无内点，产生矛盾。

**结论 2：** 无理数集在 $\mathbb{R}$ 中无内点。

---

## 15. 闭集的隔离性
**命题：** 设 $F_1, F_2 \subset \mathbb{R}^n$ 是两个不相交的闭集，证明存在不相交的开集 $G_1, G_2$ 使得 $G_1 \supset F_1, G_2 \supset F_2$。

**证明：**
1. 令 $d = d(F_1, F_2) = \inf \{ d(x, y) : x \in F_1, y \in F_2 \}$。
2. 定义开集：
   - $G_1 = \{ x \mid d(x, F_1) < \frac{d}{3} \}$
   - $G_2 = \{ y \mid d(y, F_2) < \frac{d}{3} \}$
3. 显然 $F_1 \subset G_1, F_2 \subset G_2$。
4. **反证不相交性：** 若 $G_1 \cap G_2 \neq \emptyset$，设 $z \in G_1 \cap G_2$。
   - 则 $d(z, F_1) < \frac{d}{3}$ 且 $d(z, F_2) < \frac{d}{3}$。
   - 存在 $x_1 \in F_1, y_2 \in F_2$ 使得 $d(z, x_1) < \frac{d}{3}$ 和 $d(z, y_2) < \frac{d}{3}$。
   - 由三角不等式：$d(x_1, y_2) \leq d(x_1, z) + d(z, y_2) < \frac{d}{3} + \frac{d}{3} = \frac{2d}{3} < d$。
   - 这与 $d$ 是 $F_1, F_2$ 之间的下确界矛盾。
5. 故 $G_1 \cap G_2 = \emptyset$。

## 测度论

## 开集的体积

R^n区间(矩体)
$
G\subset R^n\\
G=\cup _{k=1}^{\infty} J_k\\
J_k是左开右闭的两两不交的矩体\\
但是这样的表示法是不唯一的\\
\\\\
R^n中的开集我们该如何定义其体积\\
引理1:\\
\{I_j\}_{j=1}^{n_1},\{J_k\}_{k=1}^{n_2}是矩体\\
若\cup_{j=1}^{n_1}I_j\subset\cup _{k=1}^{n_2}J_k\\
(I_J互不相交)\\
则\sum_{j=1}^{n_1}|I_j|=|\cup_{j=1}^{n_1}I_j|\\
\leq \sum_{k=1}^{n_2}|J_k|\\
E\subseteq \Omega \subseteq R^n\\
引理2:\\
G=\cup_{j=1}^{\infty}I_j=\cup_{k=1}^{\infty}J_k,G是开集\\
(I_j两两不交左开右闭,J_k两两不交左开右闭)\\
则\sum _{j=1}^{\infty}|I_j|=\sum_{k=1}^{\infty}|J_k|\\
\forall j,k\\
我们取G_j\supset I_j,为开集\\
F_k\subset J_k为闭集\\
|G_j|<|I_j|+\frac{\epsilon}{2^{j+1}}\\
|F_k|>|J_k|-\frac{\epsilon}{2^{j+1}}\\
\cup_{j=1}^{\infty}G_j\supset \cup_{j=1}^{\infty}I_j=G=\cup_{k=1}^{\infty}J_k\supset \cup_{k=1}^{N}F_k,\forall N\\
我们也就得到了闭集\cup_{k=1}^{N}F_k的一个开覆盖\cup_{j=1}^{\infty}G_j\\
\\
根据有限覆盖定理\\
\exists M_N\\
s.t. \cup_{j=1}^{M_N}G_j\supset\cup_{k=1}^{N}F_k\\
因此\cup_{k=1}^{N}(|J_k|-\frac{\epsilon}{2^{j+1}})\leq |\cup_{k=1}^{N}F_k|\leq |\cup _{j=1}^{M_N}G_j|\leq \sum_{j=1}^{M_N}(|I_j|+\frac{\epsilon}{2^{j+1}})\\
$
### 开集体积定义的良定义性（完整证明）

设矩体统一取左开右闭形式
$$
I=\prod_{r=1}^n[a_r,b_r),\qquad |I|:=\prod_{r=1}^n(b_r-a_r).
$$

**定理：** 若开集 $G\subset\mathbb R^n$ 有两种分解
$$
G=\bigsqcup_{j=1}^{\infty}I_j=\bigsqcup_{k=1}^{\infty}J_k,
$$
其中 $I_j,J_k$ 都是左开右闭矩体且各自两两不交，则
$$
\sum_{j=1}^{\infty}|I_j|=\sum_{k=1}^{\infty}|J_k|.
$$
因此开集体积定义
$$
|G|:=\sum_{j=1}^{\infty}|I_j|
$$
与分解方式无关。

**证明：** 取任意 $\epsilon>0$。

1. 对每个 $j$，取开矩体 $G_j\supset I_j$，使
$$
|G_j|<|I_j|+\frac{\epsilon}{2^{j+1}}.
$$
2. 对每个 $k$，取闭矩体 $F_k\subset J_k$，使
$$
|F_k|>|J_k|-\frac{\epsilon}{2^{k+1}}.
$$
3. 固定 $N\in\mathbb N$，令
$$
K_N:=\bigcup_{k=1}^N F_k.
$$
则 $K_N$ 为紧集，且
$$
K_N\subset G=\bigcup_{j=1}^{\infty}I_j\subset\bigcup_{j=1}^{\infty}G_j.
$$
由 Heine-Borel 定理，存在有限指标集 $A_N\subset\mathbb N$，使
$$
K_N\subset\bigcup_{j\in A_N}G_j.
$$
4. 由单调性与有限次次可加性，
$$
\sum_{k=1}^N|F_k|
\le \left|\bigcup_{j\in A_N}G_j\right|
\le \sum_{j\in A_N}|G_j|
\le \sum_{j=1}^{\infty}\left(|I_j|+\frac{\epsilon}{2^{j+1}}\right)
=\sum_{j=1}^{\infty}|I_j|+\frac{\epsilon}{2}.
$$
而由第 2 步，
$$
\sum_{k=1}^N|J_k|-\frac{\epsilon}{2}<\sum_{k=1}^N|F_k|.
$$
合并得
$$
\sum_{k=1}^N|J_k|<\sum_{j=1}^{\infty}|I_j|+\epsilon.
$$
令 $N\to\infty$，得
$$
\sum_{k=1}^{\infty}|J_k|\le \sum_{j=1}^{\infty}|I_j|+\epsilon.
$$
由 $\epsilon$ 任意，
$$
\sum_{k=1}^{\infty}|J_k|\le \sum_{j=1}^{\infty}|I_j|.
$$

交换 $I$ 与 $J$ 的角色，同理可得
$$
\sum_{j=1}^{\infty}|I_j|\le \sum_{k=1}^{\infty}|J_k|.
$$
故
$$
\sum_{j=1}^{\infty}|I_j|=\sum_{k=1}^{\infty}|J_k|.
$$
定理证毕。

### 补充：任意开集可分解为左开右闭矩体并

**命题：** 对任意开集 $G\subset\mathbb R^n$，存在至多可数个两两不交的左开右闭矩体 $\{Q_\alpha\}$，使得
$$
G=\bigsqcup_{\alpha=1}^{\infty}Q_\alpha.
$$

**证明：**

1. 先引入二进制左开右闭矩体（dyadic cubes）：
$$
Q_{m,k}:=\prod_{r=1}^n\left[\frac{k_r}{2^m},\frac{k_r+1}{2^m}\right),
\quad m\in\mathbb N,\ k=(k_1,\dots,k_n)\in\mathbb Z^n.
$$
对固定 $m$，这些矩体两两不交，且并为整个 $\mathbb R^n$。

2. 定义
$$
\mathcal F_m:=\{Q_{m,k}:\overline{Q_{m,k}}\subset G\},
\qquad
\mathcal F:=\bigcup_{m=1}^{\infty}\mathcal F_m.
$$
因为 $\{(m,k):m\in\mathbb N,k\in\mathbb Z^n\}$ 可数，故 $\mathcal F$ 可数。

3. 证明
$$
G=\bigcup_{Q\in\mathcal F}Q.
$$
- “$\subset$”显然：每个 $Q\in\mathcal F$ 都满足 $Q\subset\overline Q\subset G$。
- “$\supset$”：任取 $x\in G$。因 $G$ 开，存在 $r>0$ 使 $B(x,r)\subset G$。取 $m$ 充分大，使
$$
2^{-m}<\frac{r}{\sqrt n}.
$$
令 $Q_{m,k}$ 为该层中包含 $x$ 的唯一二进制矩体。对任意 $y\in\overline{Q_{m,k}}$，有
$$
\|y-x\|_\infty\le 2^{-m},
\quad
\|y-x\|_2\le \sqrt n\,2^{-m}<r.
$$
故 $\overline{Q_{m,k}}\subset B(x,r)\subset G$，即 $Q_{m,k}\in\mathcal F$ 且 $x\in Q_{m,k}$。

4. 现在把可数覆盖变成两两不交分解。定义
$$
\mathcal D_1:=\mathcal F_1,
\qquad
\mathcal D_m:=\{Q\in\mathcal F_m:Q\text{ 不包含于任何 }\mathcal F_{m-1}\text{ 中的矩体}\},\ m\ge2.
$$
并令
$$
\mathcal D:=\bigcup_{m=1}^{\infty}\mathcal D_m.
$$

5. 证明 $\mathcal D$ 两两不交且并为 $G$：
- 同一层 $m$ 内本来就两两不交。
- 若 $Q\in\mathcal D_m,\ P\in\mathcal D_\ell$ 且 $m<\ell$，二进制网格性质给出：$P$ 与 $Q$ 要么不交，要么 $P\subset Q$。若 $P\subset Q$，则 $P$ 被上一层某个矩体（即 $Q$ 或其同层祖先）包含，和 $P\in\mathcal D_\ell$ 矛盾。所以必不交。
- 每个 $\mathcal F_m$ 中被删去的矩体都包含在更粗层矩体里，因此不影响并集，故
$$
\bigcup_{Q\in\mathcal D}Q=\bigcup_{Q\in\mathcal F}Q=G.
$$

因此 $G$ 被表示成可数个两两不交左开右闭矩体的并。证毕。

开集体积的性质:

$$
\begin{aligned}
&(1)\ G=\emptyset\Rightarrow |G|=0,\quad G\neq\emptyset\Rightarrow |G|\neq 0.\\
&(2)\ \text{若 }G_1\subset G_2,\ \text{则 }|G_1|\le |G_2|.\\
&(3)\ \left|\bigcup_{j=1}^{\infty}G_j\right|\le \sum_{j=1}^{\infty}|G_j|.\\
&(4)\ \text{若 }G_j\ \text{两两不交, 则 }\left|\bigcup_{j=1}^{\infty}G_j\right|=\sum_{j=1}^{\infty}|G_j|.\\
&(5)\ 平移不变性 G+\{x_0\}=\{x+x_0,x\in G\}\\
&~~~~~~~~~|G|=|G+\{x_0\}|
\\
&(6)\ 旋转不变性:G'是G旋转之后得到的集合,则|G|=|G'|\\
\end{aligned}
$$

其中 (2) 可由单调性（或仿照良定义性证明）得到，(4) 由 (3) 与有限可加性/极限过程推出。下面补充 (3) 的完整证明。

**证明 (3)：** 设
$$
U:=\bigcup_{j=1}^{\infty}G_j.
$$
若右端 $\sum_{j=1}^{\infty}|G_j|=+\infty$，结论显然。以下设其有限。

把每个 $G_j$ 分解为两两不交左开右闭矩体：
$$
G_j=\bigsqcup_{m=1}^{\infty}I_{j,m},\qquad |G_j|=\sum_{m=1}^{\infty}|I_{j,m}|.
$$
也把 $U$ 分解为
$$
U=\bigsqcup_{r=1}^{\infty}J_r,\qquad |U|=\sum_{r=1}^{\infty}|J_r|.
$$

任取 $\epsilon>0$。对每个 $r$，取闭矩体 $F_r\subset J_r$，使
$$
|F_r|>|J_r|-\frac{\epsilon}{2^{r+1}}.
$$

固定 $N\in\mathbb N$，令
$$
K_N:=\bigcup_{r=1}^{N}F_r.
$$
则 $K_N$ 为紧集，且
$$
K_N\subset U\subset\bigcup_{j,m}I_{j,m}.
$$
由 Heine-Borel 定理，存在有限指标集 $A_N$，使
$$
K_N\subset\bigcup_{(j,m)\in A_N}I_{j,m}.
$$

于是
$$
\sum_{r=1}^{N}|F_r|
\le \left|\bigcup_{(j,m)\in A_N}I_{j,m}\right|
\le \sum_{(j,m)\in A_N}|I_{j,m}|
\le \sum_{j=1}^{\infty}\sum_{m=1}^{\infty}|I_{j,m}|
=\sum_{j=1}^{\infty}|G_j|.
$$
另一方面
$$
\sum_{r=1}^{N}|J_r|-\frac{\epsilon}{2}<\sum_{r=1}^{N}|F_r|.
$$
合并得
$$
\sum_{r=1}^{N}|J_r|<\sum_{j=1}^{\infty}|G_j|+\frac{\epsilon}{2}.
$$
令 $N\to\infty$，得
$$
|U|=\sum_{r=1}^{\infty}|J_r|\le \sum_{j=1}^{\infty}|G_j|+\frac{\epsilon}{2}.
$$
再令 $\epsilon\downarrow0$，得到
$$
\left|\bigcup_{j=1}^{\infty}G_j\right|\le \sum_{j=1}^{\infty}|G_j|.
$$

**证明 (4)（直接由定义）：** 设 $\{G_j\}_{j\ge1}$ 两两不交，且每个 $G_j$ 都是开集。

按开集体积定义，对每个 $j$，可写
$$
G_j=\bigsqcup_{m=1}^{\infty}I_{j,m},
\qquad
|G_j|=\sum_{m=1}^{\infty}|I_{j,m}|,
$$
其中 $I_{j,m}$ 为左开右闭矩体，且对固定 $j$ 两两不交。

又因为 $G_i\cap G_j=\varnothing\ (i\ne j)$，故不同 $j$ 的矩体也互不相交，于是全体 $\{I_{j,m}\}_{j,m}$ 两两不交，并且
$$
\bigcup_{j=1}^{\infty}G_j
=\bigsqcup_{j=1}^{\infty}\bigsqcup_{m=1}^{\infty}I_{j,m}.
$$

再按定义计算并集体积：
$$
\left|\bigcup_{j=1}^{\infty}G_j\right|
=\sum_{j=1}^{\infty}\sum_{m=1}^{\infty}|I_{j,m}|
=\sum_{j=1}^{\infty}|G_j|.
$$

即
$$
\left|\bigcup_{j=1}^{\infty}G_j\right|=\sum_{j=1}^{\infty}|G_j|.
$$
证毕。



$E\subseteq R^n(\lambda >0)\\
\lambda E=\{\lambda x,x\in E\}\\
|\lambda I|=\lambda ^n|I|\\$

## 从开集到一般

外侧度
$m^{*}(E)=\inf{|G|,G\supset E,G开}\\
e.g. m^{*}(\{x_0\})=0,m^{*}(Q)=0\\
下面计算Cantor集的外测度\\
m^*(C)=0\\\\
性质:\\
1.(非负性)m^*(\emptyset)=0,M^*(E)\geq 0\\
2.(单调性)E_1\subset E_2\Rightarrow m^*(E_1)\le m^*(E_2)\\
3.(次可数可加性)m^*(\bigcup_{j=1}^{\infty}E_j)\leq \sum_{j=1}^{\infty}m^*(E_j)\\
证明:\\
\forall E_k,\exists G_k\supset E_k\\
s.t. m^*(G_k)\leq m^*(E_k)+\frac{\epsilon}{2^k}\\
m^*(\bigcup_{k=1}^{\infty}E_k)\leq m^*(\bigcup_{k=1}^{\infty}G_k)\leq \sum_{k=1}^{\infty}m^*(G_k)\leq \sum_{k=1}^{\infty}m^*(E_k)+\sum_{k=1}^{\infty}\frac{\epsilon}{2^k}\\
令\epsilon \to 0,即知次可数可加性成立\\
4.(分离可数可加性)\\
若\exists G_k\supset E_k(G_k两两不交)\\
则m^*(\bigcup_{k=1}^{\infty}E_k)=\sum_{k=1}^{\infty}m^*(E_k)\\
证明:\\
令E=\cup_{k=1}^{\infty}E_k\\
则\exists G,\\
s.t. m^*(G)\leq m^*(E)+\epsilon \\
令V_k=G\cap G_k\supset E_k\\
则V_k是开集,且V_k两两不交\\
则m^*(\cup_{k=1}^{\infty}V_k)=\sum_{k=1}^{\infty}m^*(V_k)\\
RHS\geq \sum_{k=1}^{\infty}m^*(E_k)\\
LHS\leq m^*(G)\leq m^*(E)+\epsilon\\
5.(平移不变性)m^*(E)=m^*(E+x_0)\\
\\\\
e.g.\\
m^*(Q)=m^*(\cup_{k=1}^{\infty}{r_k})\leq \sum_{k=1}^{\infty}m^*(\{r_k\})=0\\
又由(1)知m^*(Q)\geq 0\\
故知m^*(Q)=0\\
(注意这里不能直接用性质(4)来证明)\\
(因为性质四种的可加性是针对开集不交而言的)\\
\\\\
\forall r_k\in Q\cap (0,1)\\
令B_k=B(r_k,\frac{1}{2^{k+1}})\cap (0,1)\\
m^*(B_k)=|B_k|<\frac{1}{2^{k+1}}\\
令G=\cup_{k=1}^{\infty}B_k仍未开集\\
则G\supset Q\cap (0,1)\\
m^*(G)=|G|\leq \frac{1}{2^2}+\cdots=\frac{1}{2}\\
$
## (不可测集)

$\exists集合E_1,E_2\\
E_1\cap E_2=\emptyset\\
但m^*(E_1\cup E_2)\ne m^*(E_1)+m^*(E_2)\\
\forall x\in (0,1),令R_{x}=\{y\in (0,1):y-x\in Q\}
若R_{x_1}\ne R_{x_2},R_{x_1}\cap R_{x_2}=\emptyset\\
则x_1-y,x_2-y\in Q\\
\\
(0,1)\subset \cup_{x\in(0,1)}R_x\\
\\
对每个R_x,都任取一点,\\
记S为这些取出的点构成的集合.\\
S\subset (0,1)\\
令r_k\in (-1,1)为有理数\\
S_k=S+r_k\\
则(0,1)\subset \cup_{k=1}^{+\infty}S_k\\
$
# 证明：Vitali 集不满足外测度的可数可加性

## 1. 构造准备
设 $V \subset [0, 1]$ 是一个 **Vitali 集**。
令 $\mathbb{Q} \cap [-1, 1] = \{q_1, q_2, q_3, \dots\}$ 是区间 $[-1, 1]$ 内所有有理数的一个枚举（可数集）。

构造平移集合序列：
$$V_k = V + q_k = \{v + q_k \mid v \in V\}, \quad k = 1, 2, 3, \dots$$

## 2. 集合性质
根据 Vitali 集的构造，这些集合具有以下关键性质：

1.  **两两不相交**：
    若存在 $x \in V_i \cap V_j$，则 $x = v_i + q_i = v_j + q_j$，推导出 $v_i - v_j = q_j - q_i \in \mathbb{Q}$。
    根据 Vitali 集的等价类定义，这说明 $v_i = v_j$，从而 $q_i = q_j$。
2.  **包含关系**：
    * 因为 $V \subset [0, 1]$ 且 $q_k \in [-1, 1]$，故 $\bigcup_{k=1}^{\infty} V_k \subset [-1, 2]$。
    * 根据等价类覆盖性质，$[0, 1] \subset \bigcup_{k=1}^{\infty} V_k$。

## 3. 核心证明步骤

### 第一步：利用外测度的平移不变性
对于每一个 $k \in \mathbb{N}$，由外测度的平移不变性可知：
$$m^*(V_k) = m^*(V + q_k) = m^*(V)$$

### 第二步：利用单调性进行两面夹击
令并集 $S = \bigcup_{k=1}^{\infty} V_k$。利用外测度的单调性：
1.  **下方估计**：因为 $[0, 1] \subset S$，所以 $m^*(S) \ge m^*([0, 1]) = 1$。
2.  **上方估计**：因为 $S \subset [-1, 2]$，所以 $m^*(S) \le m^*([-1, 2]) = 3$。

即：
$$1 \le m^*\left(\bigcup_{k=1}^{\infty} V_k\right) \le 3$$

### 第三步：验证可数可加性的矛盾
**假设**外测度 $m^*$ 满足可数可加性，则：
$$m^*\left(\bigcup_{k=1}^{\infty} V_k\right) = \sum_{k=1}^{\infty} m^*(V_k) = \sum_{k=1}^{\infty} m^*(V)$$

我们对 $m^*(V)$ 的取值进行讨论：
* **若 $m^*(V) = 0$**：
    则 $\sum_{k=1}^{\infty} 0 = 0$，这与 $m^*(S) \ge 1$ 矛盾。
* **若 $m^*(V) > 0$**：
    则 $\sum_{k=1}^{\infty} m^*(V) = +\infty$，这与 $m^*(S) \le 3$ 矛盾。

## 4. 结论
由于 $m^*(V)$ 无论取何值都会导致逻辑矛盾，唯一的解释是：**外测度对于这一组互不相交的集合序列不满足可数可加性**。

即：
$$m^*\left(\bigcup_{k=1}^{\infty} V_k\right) \neq \sum_{k=1}^{\infty} m^*(V_k)$$

## Caratheodory 条件
$m^*(T)=m^*(T\cap E)+m^*(T\cap E^c)\,E\subseteq R^n,\forall T\subseteq R^n\\
若E满足Caratheodory条件,则E是可测集\\
记m(E)=m^*(E)\\
称之为E的Lebesgue测度\\
若E_1\cap E_2 =\emptyset \\
为上述Vitali集\\
下面证明Vitali集不满足Caratheodory条件\\
对E_1,取T=E_1\cup E_2\\
则T\cap E_1=E_1,T\cap E_1^c=E_2\\
故有m^*(T)\ne m^*(E_1)+m^*(E_2)\\
故知E_1不可测\\$

## 哪些集合可测？

### Borel集都是可测集

#### 可测集全体构成sigma代数

### Carathéodory 条件的简化证明

**已知条件：**
对于任意开矩体 $I \subseteq \mathbb{R}^n$，满足 Carathéodory 条件：
$$m^*(I) = m^*(I \cap E) + m^*(I \cap E^c)$$

**求证：**
该条件可以放宽到任意测试集 $T \subseteq \mathbb{R}^n$，即：
$$m^*(T) = m^*(T \cap E) + m^*(T \cap E^c)$$

---

**证明过程：**

**第一步：明确“天然成立”的不等式（次可加性）**
根据集合的分解 $T = (T \cap E) \cup (T \cap E^c)$，以及 Lebesgue 外测度 $m^*$ 的次可加性，对于任意集合 $T$，天然有：
$$m^*(T) \leq m^*(T \cap E) + m^*(T \cap E^c)$$
因此，为了证明等式成立，我们只需要证明反向不等式即可，即往证：
$$m^*(T \cap E) + m^*(T \cap E^c) \leq m^*(T)$$

**第二步：利用外测度定义构造开矩体覆盖**
根据 Lebesgue 外测度的定义，对于任意给定的 $\epsilon > 0$，存在一列开矩体 $\{I_i\}_{i=1}^{\infty}$ 覆盖 $T$（即 $T \subseteq \bigcup_{i=1}^{\infty} I_i$），使得：
$$\sum_{i=1}^{\infty} m^*(I_i) \leq m^*(T) + \epsilon$$

**第三步：集合交集的包含关系**
因为 $T \subseteq \bigcup_{i=1}^{\infty} I_i$，两边同时取与 $E$ 和 $E^c$ 的交集，包含关系依然成立：
$$T \cap E \subseteq \bigcup_{i=1}^{\infty} (I_i \cap E)$$
$$T \cap E^c \subseteq \bigcup_{i=1}^{\infty} (I_i \cap E^c)$$

**第四步：利用外测度的单调性与次可加性放缩**
对上述两个包含式两边取外测度，利用单调性（左边 $\leq$ 右边的外测度）和次可加性（并集的外测度 $\leq$ 外测度之和），得到：
$$m^*(T \cap E) \leq \sum_{i=1}^{\infty} m^*(I_i \cap E)$$
$$m^*(T \cap E^c) \leq \sum_{i=1}^{\infty} m^*(I_i \cap E^c)$$

将两式左右两端分别相加：
$$m^*(T \cap E) + m^*(T \cap E^c) \leq \sum_{i=1}^{\infty} \left[ m^*(I_i \cap E) + m^*(I_i \cap E^c) \right]$$

**第五步：代入已知条件并得出结论**
已知对于开矩体 $I_i$，满足 $m^*(I_i \cap E) + m^*(I_i \cap E^c) = m^*(I_i)$。代入上式得：
$$m^*(T \cap E) + m^*(T \cap E^c) \leq \sum_{i=1}^{\infty} m^*(I_i)$$

结合第二步中的覆盖不等式 $\sum_{i=1}^{\infty} m^*(I_i) \leq m^*(T) + \epsilon$，可得：
$$m^*(T \cap E) + m^*(T \cap E^c) \leq m^*(T) + \epsilon$$

因为 $\epsilon > 0$ 是任意的，令 $\epsilon \to 0$，得到反向严格不等式：
$$m^*(T \cap E) + m^*(T \cap E^c) \leq m^*(T)$$

**结论：**
结合第一步天然成立的不等式（即 $\leq$），既有 $\leq$ 又有 $\geq$，故等式严格成立：
$$m^*(T) = m^*(T \cap E) + m^*(T \cap E^c)$$
证毕。

e.g. E可测一定可以推导出$E^c$可测
0测集天然满足Caratheodory条件,0测集一定可测
$prove:
m^*(T)=m^*(T\cap E)+m^*(T\cap E^c)\\
一方面显然有:\\
m^*(T)\leq m^*(T\cap E)+m^*(T\cap E^c)\\
另一方面RHS\leq m^*(E)+m^*(T)=m^*(T)\\
$
### 证明：若 $E_1, E_2$ 可测，则 $E_1 \cup E_2$ 与 $E_1 \cap E_2$ 可测

**前置知识（Carathéodory 条件）：**
集合 $E$ 可测，当且仅当对任意测试集 $T \subseteq \mathbb{R}^n$，都有：
$$m^*(T) = m^*(T \cap E) + m^*(T \cap E^c)$$
如前所述，由于外测度的次可加性天然成立，我们实际只需证明不等式 $\geq$ 成立即可。

---

#### 1. 证明 $E_1 \cup E_2$ 可测

对于任意测试集 $T$，我们需要证明：
$$m^*(T) \geq m^*(T \cap (E_1 \cup E_2)) + m^*(T \cap (E_1 \cup E_2)^c)$$

**第一步：利用 $E_1$ 的可测性展开第一项**
因为 $E_1$ 可测，我们可以把 $T \cap (E_1 \cup E_2)$ 整体作为一个测试集，用 $E_1$ 对其进行分割：
$$m^*(T \cap (E_1 \cup E_2)) = m^*((T \cap (E_1 \cup E_2)) \cap E_1) + m^*((T \cap (E_1 \cup E_2)) \cap E_1^c)$$

利用集合分配律化简括号内的交集关系：
- $(T \cap (E_1 \cup E_2)) \cap E_1 = T \cap E_1$
- $(T \cap (E_1 \cup E_2)) \cap E_1^c = T \cap (E_2 \cap E_1^c)$

所以得到：
$$m^*(T \cap (E_1 \cup E_2)) = m^*(T \cap E_1) + m^*(T \cap E_2 \cap E_1^c)$$

**第二步：代回原式并利用 $E_2$ 的可测性**
根据德·摩根定律，$(E_1 \cup E_2)^c = E_1^c \cap E_2^c$。
我们将第一步化简的结果，代入我们需要证明的不等式右端：
$$m^*(T \cap (E_1 \cup E_2)) + m^*(T \cap (E_1 \cup E_2)^c)$$
$$= \left[ m^*(T \cap E_1) + m^*(T \cap E_2 \cap E_1^c) \right] + m^*(T \cap E_1^c \cap E_2^c)$$
重新结合一下（把带 $E_1^c$ 的项放一起）：
$$= m^*(T \cap E_1) + \left[ m^*(T \cap E_1^c \cap E_2) + m^*(T \cap E_1^c \cap E_2^c) \right]$$

观察方括号内的两项！因为 **$E_2$ 是可测的**，我们可以把 $T \cap E_1^c$ 作为测试集，用 $E_2$ 对其进行分割：
$$m^*(T \cap E_1^c) = m^*((T \cap E_1^c) \cap E_2) + m^*((T \cap E_1^c) \cap E_2^c)$$

你会发现方括号内的和正好等于 $m^*(T \cap E_1^c)$。整体替换后得到：
$$= m^*(T \cap E_1) + m^*(T \cap E_1^c)$$

**第三步：利用 $E_1$ 的可测性得出最终结论**
因为 **$E_1$ 也是可测的**，对测试集 $T$ 使用 $E_1$ 分割，必然有：
$$m^*(T \cap E_1) + m^*(T \cap E_1^c) = m^*(T)$$

至此我们推导完毕：
$$m^*(T \cap (E_1 \cup E_2)) + m^*(T \cap (E_1 \cup E_2)^c) = m^*(T)$$
即 Carathéodory 条件成立，故 $E_1 \cup E_2$ 可测。

---

#### 2. 证明 $E_1 \cap E_2$ 可测

证明交集可测最优雅的方法是不再去死磕定义，而是利用补集和德·摩根定律（集合的代数结构）。

**第一步：证明可测集的补集依然可测**
回顾 Carathéodory 条件：$E$ 可测即对任意 $T$，有
$$m^*(T) = m^*(T \cap E) + m^*(T \cap E^c)$$
这个等式中 $E$ 和 $E^c$ 的地位是完全对称的（加法交换律）。也就是说，如果我们把 $E^c$ 看作要去检验的集合，它的补集正好是 $E$。
等式完全一样，因此：**若 $E$ 可测，则 $E^c$ 必然可测**。

**第二步：利用德·摩根定律转化**
根据德·摩根定律（De Morgan's laws）：
$$E_1 \cap E_2 = (E_1^c \cup E_2^c)^c$$

**第三步：逻辑链条闭环**
1. 已知 $E_1, E_2$ 可测 $\implies$ 它们的补集 $E_1^c, E_2^c$ 均可测（由第一步）。
2. 既然 $E_1^c, E_2^c$ 均可测 $\implies$ 它们的并集 $E_1^c \cup E_2^c$ 也可测（由前面第 1 部分证明的**并集可测性**定理）。
3. 既然 $E_1^c \cup E_2^c$ 可测 $\implies$ 它再取一次补集 $(E_1^c \cup E_2^c)^c$ 依然可测（再次利用第一步）。
4. 所以，$E_1 \cap E_2$ 可测。

证毕。

Property:
$E_1,\cdots 可测（可数个可测集）\\
\cup_{i=1}^{\infty} E_i可测\\
若A_1,\cdots ,A_n互不相交\\
则\forall i,\exists E_i\supset A_i\\
s.t. E_i\cap (\cup_{i\ne i_0} A_i)=\emptyset\\
则m^*(T\cap \cup_{i=1}^n A_i)=\sum_{i=1}^n m^*(T\cap A_i)\\
$
证明:
$m^*(T)=m^*(T\cap _{i=1}^{k}E_k)+m^*(T\cap (\cup_{i=1}^{k}E_i)^c)\\
\cup_{i=1}^{k}E_i可测\\
设E_i两两不交\\
=\sum_{i=1}^{k}m^*(T\cap E_i)+m^*(T\cap (\cup_{i=1}^{k}E_i)^c)
\\
\geq \sum_{i=1}^{k}m^*(T\cap E_i)+m^*(T\cap E^c)\\
\geq \sum_{i=1}^{\infty}m^*(T\cap E_i)+m^*(T\cap E^c)\\
\geq m^*(T\cap \cup_{i=1}^{\infty} E_i)+m^*(T\cap E^c)$



### 证明：可数个可测集的并、交仍然是可测集

**前置定理：**
1. 若 $A, B$ 可测，则 $A \cup B, A \cap B, A \setminus B$ 均可测。
2. 外测度的次可加性天然成立，故证明任意集合 $E$ 可测，只需对任意测试集 $T$ 证明：
   $$m^*(T) \ge m^*(T \cap E) + m^*(T \cap E^c)$$

---

#### 1. 证明可数个可测集的并是可测集

设 $\{E_i\}_{i=1}^{\infty}$ 是一列可测集，令 $E = \bigcup_{i=1}^{\infty} E_i$。往证 $E$ 可测。

**第一步：“正交化”构造互不相交的可测集**
为了方便计算，我们构造一个新集合序列 $\{A_i\}$，使得它们互不相交，且它们的并集就是 $E$：
令 $A_1 = E_1$
令 $A_2 = E_2 \setminus E_1 = E_2 \cap E_1^c$
$\dots$
令 $A_n = E_n \setminus \bigcup_{i=1}^{n-1} E_i = E_n \cap E_{n-1}^c \cap \dots \cap E_1^c$

因为有限个可测集的交和补仍是可测集，所以**所有的 $A_n$ 都是可测集**。
并且显然有：$\bigcup_{i=1}^{\infty} A_i = \bigcup_{i=1}^{\infty} E_i = E$，且 $A_i \cap A_j = \emptyset \ (i \ne j)$。

**第二步：利用有限并的可测性进行递推**
令 $S_n = \bigcup_{i=1}^n A_i$。因为有限个可测集的并是可测集，所以 $S_n$ 是可测的。
对于任意测试集 $T$，根据 $S_n$ 的可测性，有：
$$m^*(T) = m^*(T \cap S_n) + m^*(T \cap S_n^c)$$

因为 $S_n \subseteq E$，取补集后方向反转，即 $E^c \subseteq S_n^c$。
所以 $T \cap E^c \subseteq T \cap S_n^c$。
由外测度的单调性可得：$m^*(T \cap S_n^c) \ge m^*(T \cap E^c)$。
代入上式，我们得到一个重要的不等式：
$$m^*(T) \ge m^*(T \cap S_n) + m^*(T \cap E^c) \quad \text{(*)}$$

**第三步：展开 $m^*(T \cap S_n)$**
由于 $A_n$ 可测，我们用 $A_n$ 对测试集 $T \cap S_n$ 进行分割：
$$m^*(T \cap S_n) = m^*((T \cap S_n) \cap A_n) + m^*((T \cap S_n) \cap A_n^c)$$
因为 $A_n \subseteq S_n$，所以第一项就是 $T \cap A_n$。
因为 $A_i$ 互不相交，所以 $S_n \cap A_n^c = S_{n-1}$，第二项就是 $T \cap S_{n-1}$。
即：
$$m^*(T \cap S_n) = m^*(T \cap A_n) + m^*(T \cap S_{n-1})$$
不断向下递推，最终可得：
$$m^*(T \cap S_n) = \sum_{i=1}^n m^*(T \cap A_i)$$

**第四步：取极限并利用外测度的次可加性**
将第三步的结论代回我们之前的结论 $(*)$：
$$m^*(T) \ge \sum_{i=1}^n m^*(T \cap A_i) + m^*(T \cap E^c)$$

上式对任意有限的 $n$ 都成立。令 $n \to \infty$，得到：
$$m^*(T) \ge \sum_{i=1}^{\infty} m^*(T \cap A_i) + m^*(T \cap E^c)$$

根据外测度的**可数次可加性**：
$$m^*(T \cap E) = m^*\left(T \cap \bigcup_{i=1}^{\infty} A_i\right) = m^*\left(\bigcup_{i=1}^{\infty} (T \cap A_i)\right) \le \sum_{i=1}^{\infty} m^*(T \cap A_i)$$

把这个放缩关系代入上式的级数部分：
$$m^*(T) \ge m^*(T \cap E) + m^*(T \cap E^c)$$
至此，证明了 Carathéodory 条件，故 $E = \bigcup_{i=1}^{\infty} E_i$ 可测。

---

#### 2. 证明可数个可测集的交是可测集

证明了“可数并可测”后，证明“可数交可测”就变成了一行代码的事（继续使用德·摩根定律）。

设 $\{E_i\}_{i=1}^{\infty}$ 是一列可测集，令 $E = \bigcap_{i=1}^{\infty} E_i$。往证 $E$ 可测。

**证明：**
1. 因为 $E_i$ 可测，所以其补集 $E_i^c$ 也可测。
2. 根据德·摩根定律，交集的补集等于补集的并集：
   $$E^c = \left( \bigcap_{i=1}^{\infty} E_i \right)^c = \bigcup_{i=1}^{\infty} E_i^c$$
3. 根据我们在第 1 部分刚刚证明的定理（可数个可测集的并仍可测），由于 $\{E_i^c\}$ 是一列可测集，它们的并集 $\bigcup_{i=1}^{\infty} E_i^c$ 必然可测。
4. 也就是说，$E^c$ 是可测的。
5. 既然 $E^c$ 可测，那么它的补集 $(E^c)^c = E$ 必然可测。




副产品:

1.若E_i两两不交,可测.则
$$m^*(T\cap \cup_{i=1}^{\infty} E_i)=\sum_{i=1}^{\infty} m^*(T\cap E_i)$$
（直到这里我们才证明了可测集的外侧度具有分离可数可加性）
特别地,取T=R^n
则$m^*(\cup_{i=1}^{\infty} E_i)=\sum_{i=1}^{\infty} m^*(T\cap E_i)$


综上可知可测集全体构成\sigma 代数
可测集的上下极限仍然可测
若$E_1\subset E_2\subset \cdots \subset \cdots 均为可测集列\\
则m(\lim_{i\to \infty}E_i)=\lim_{i\to \infty}m(E_i) \\$

令$S_1=E_1,S_2=E_2/E_1,\cdots$
则
$m(\cup_{i=1}^{\infty}S_i)=\lim_{i\to \infty}m(S_i)=\lim_{k\to \infty}\sum _{i=1}^{k}m(S_i)\\
$
$E_1\supset E_2\supset \cdots E_n\\
为可测下降集合列,且存在k_0,s.t. m(E_{k_0})<\infty\\
m(\lim_{k\to \infty}E_k)=\lim_{k\to \infty}m(E_k)\\$







### 证明：递减可测集合列的测度连续性

**已知条件：**
1. $\{E_k\}$ 为可测下降集合列，即 $E_1 \supset E_2 \supset \cdots \supset E_n \supset \cdots$
2. 存在某个 $k_0$，使得 $m(E_{k_0}) < \infty$

**求证：**
$$m\left(\lim_{k\to \infty}E_k\right) = \lim_{k\to \infty}m(E_k)$$

---

**证明过程：**

**第一步：明确极限的定义并简化序列**
对于递减集合列，其极限集定义为全部集合的交集：
$$E = \lim_{k\to \infty}E_k = \bigcap_{k=1}^\infty E_k$$
因为交集与前 $k_0-1$ 项无关，显然有 $E = \bigcap_{k=k_0}^\infty E_k$，且 $E \subseteq E_{k_0}$。

**第二步：利用“正交化”构造互不相交的集合**
测度论最常用的技巧就是把包含关系变成不相交关系。我们构造一个环面序列：
令 $A_k = E_k \setminus E_{k+1} \quad (k \ge k_0)$
因为 $\{E_k\}$ 是递减的，所以这些“圆环” $A_k$ 彼此互不相交，且全都是可测集。

观察 $E_{k_0}$ 的构成，它可以被完全拆分为最终的“核心” $E$ 和外面一圈一圈的“圆环” $A_k$ 的并集：
$$E_{k_0} = E \cup \left( \bigcup_{k=k_0}^\infty A_k \right)$$
由于 $E$ 与所有的 $A_k$ 都互不相交，且所有的 $A_k$ 之间也互不相交，这是一个**互不相交的并集**。

**第三步：利用测度的可数可加性**
对上式两边取测度，利用可数可加性得：
$$m(E_{k_0}) = m(E) + \sum_{k=k_0}^\infty m(A_k)$$

因为已知 $m(E_{k_0}) < \infty$，且 $m(E) \le m(E_{k_0})$，所以 $m(E)$ 也是有限的。我们可以合法地把 $m(E)$ 移项（这一步就是有限测度条件的威力）：
$$m(E_{k_0}) - m(E) = \sum_{k=k_0}^\infty m(A_k) \quad \text{--- (式 1)}$$

**第四步：展开级数并求极限（裂项相消）**
现在我们来算等式右边的无穷级数。级数的定义就是前 $n$ 项和的极限：
$$\sum_{k=k_0}^\infty m(A_k) = \lim_{n\to \infty} \sum_{k=k_0}^{n-1} m(E_k \setminus E_{k+1})$$

由于 $E_{k+1} \subseteq E_k$，且 $m(E_k) \le m(E_{k_0}) < \infty$，满足有限测度的减法法则：$m(E_k \setminus E_{k+1}) = m(E_k) - m(E_{k+1})$。代入上式：
$$\sum_{k=k_0}^\infty m(A_k) = \lim_{n\to \infty} \sum_{k=k_0}^{n-1} \left[ m(E_k) - m(E_{k+1}) \right]$$
这是一个裂项相消（Telescoping sum）求和：
$$= \lim_{n\to \infty} \left[ (m(E_{k_0}) - m(E_{k_0+1})) + (m(E_{k_0+1}) - m(E_{k_0+2})) + \cdots + (m(E_{n-1}) - m(E_n)) \right]$$
中间项全部抵消，只剩下头和尾：
$$= \lim_{n\to \infty} \left[ m(E_{k_0}) - m(E_n) \right] = m(E_{k_0}) - \lim_{n\to \infty} m(E_n)$$

**第五步：得出结论**
将第四步算出的级数结果代回 **(式 1)** 中：
$$m(E_{k_0}) - m(E) = m(E_{k_0}) - \lim_{n\to \infty} m(E_n)$$

因为 $m(E_{k_0})$ 是一个有限的实数（$<\infty$），我们可以同时在等式两边消去它，得到：
$$-m(E) = -\lim_{n\to \infty} m(E_n)$$
即：
$$m(E) = \lim_{n\to \infty} m(E_n)$$
证毕。


th:Borel集都可测
证明:

$我们只需证明所有的开集都可测\\
G=\cup_{i=1}^{\infty}I_i\\
I_i是两两不交的左开右闭矩体的并\\
只需证明矩体可测\\
显然我们可以直接构造开矩体和半开矩体的差集（也即矩体的边界）是零测集\\
故我们只需要证明开矩体可测即可\\
考虑Caratheodory条件:\\
m^*(T)\leq m^*(T\cap I)+m^*(T\cap I^c)\\
\leq m^*(T\cap I)+m^*(T\cap \partial I)+m^*(T\cap \overline{I}^c)\\
=m^*(T\cap I)+m^*(T\cap \overline{I}^c)\\
=m^*(T\cap I)+m^*(T\cap (I\cup \overline{I}^c))\\
\leq m^*(T)\\$

### 可测集不都是Borel集
$
\begin{aligned}
&该问题通常有两种解决方案:\\
&法一：证明可测集的势不等于Borel集的势\\
&法二:考虑用Cantor函数直接进行反例构造\\
\end{aligned}
$
### 小结论
$\forall E\subset R^n可测\\
\forall \epsilon >0\\
\exists G是开集\\
s.t. m(G/E)<\epsilon \\
其中G\supset E\\  
\\
对称的:\\
\forall E\subset R^n 可测,\\
\forall \epsilon >0\\
\exists F\subset E为闭集\\
s.t. m(E/F)<\epsilon \\$


证明:
$
若m(E)<+\infty\\
由定义,\forall \epsilon >0\\
\exists G\supset E,\\
m(G)<m(E)+\epsilon \\
则m(G/E)<\epsilon \\
\\
若m(E)=+\infty\\
将E写作E=\cup_{i=1}^{+\infty}E_i\\
E_i互不相交,有界,可测\\
令E_1=E\cap B(0,1)\\
E_2=E\cap B(0,2)/B(0,1)\\
\vdots\\
\forall \epsilon >0\\
\exists G_is.t. \\
m(G_i)<m(E_i)+\frac{\epsilon }{2^i}\\
G_i\supset E_i\\
令G=\cup_{i=1}^{+\infty}m(G_i)\\
\leq \sum_{i=1}^{+\infty} (m(E_i)+\frac{\epsilon}{2^i})=mE+\epsilon\\
$
### 下面条件等价
$
1.E可测\\
2.\exists 零测集e,G_{\delta }集G,s.t. E\cup e=G\\
3.\exists 零测集N,F_{\sigma }集F,s.t. F\cup N=E\\
4.\exists G_{\delta }集G\supset E,F_{\sigma }集F\supset E,s.t. m(G/F)=0\\
\\
$
证明:
$
1\to 2:\\
由上一个小标题处的结论可知:\\
\forall k\exists G_ k 为开集\\
s.t. G_k\supset E,m(G_k/E)<\frac{1}{k}\\
则令G=\cup_{k=1}^{\infty}G_k即可\\
$
### 定理: m(A\times B)= m(A)m(B)

$若A,B有界,可测\\
1.若A,B为矩体,\\
则A\times B也是矩体\\
则显然是吧\\
2.若A,B为开集\\
将A,B分解为可数个矩体的并容易证明\\
3.若A,B为G_{\delta }集\\
则A\times B的话\\
A\times B=\cap _{n=1}^{+\infty}G_n'\times G_n''\\
m(A\times B)=\lim_{n\to \infty} \sum_{i=1}^{n} m(G_n')\times G_n'')\\
=m(A)m(B) (这里使用了下降序列的测度和极限符号可以交换顺序)\\
4.若A,B为一般的可测集\\
则对于A，B而言，\\
\exists G_{\delta }集 G',G''\\
m(G'/A)=0,m(G''/B)=0\\
G'\times G''=A\times B\cup G'/A\times B\cup A\times G''/B\cup G'/A\times G''/B\\
故知m(A\times B)=m(G'\times G'')=m(G')\times m(G'')=m(A)m(B)\\
5.若A，B中有其一无界\\
若A=\cup _{i=1}^{+\infty}A_i\\
B=\cup_{i=1}^{+\infty}B_i\\
A_i互不相交有界可测\\
B_i互不相交有界可测\\
A\times B=\cup_{i,j} A_i\times B_j\\
m(A\times B)=\sum_{i,j}m(A_i)m(B_j)\\
=\sum_{i=1}^{+\infty}m(A_i)\times \sum_{j=1}^{+\infty}m(B_j)\\$


## 截口
$
对于E\subseteq R^{p+q}\\
定义E_{x_0}=\{y\in R^{q},(x_0,y_0)\in E\}$














