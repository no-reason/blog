# 无约束优化

## 最优性条件

一阶必要性条件

二阶必要性条件

二阶充分性条件

稳定点(数分中的驻点)

## 线搜索算法

### 精确线搜索

一般不用除非有显式解

### 非精确线搜索

### Armijo线搜索 -- 因为比较简单有时候会用

$
\begin{aligned}
&取步长\alpha _{k}=\overline{\alpha }\beta^{m_{k}}\\
&m_{k}是满足下式的第一个非负整数m\\
&f(x^{k}+\overline{\alpha}\beta^{m}d^{k})-f(x^{k})\le \rho \overline{\alpha}\beta^{m}\nabla f(x^{k})^{T}d^{k}\\
&其中\overline{\alpha}>0,\beta \in (0,1),\rho \in (0,1)\\
\end{aligned}
$

### Goldstein线搜索。-- 几乎不怎么用

$
\begin{aligned}
&(1-\rho)\overline{\alpha}\beta^{m}\nabla f(x^{k})^{T}d^{k}\le f(x^{k}+\overline{\alpha}\beta^{m}d^{k})-f(x^{k})\le \rho \overline{\alpha}\beta^{m}\nabla f(x^{k})^{T}d^{k}\\
\end{aligned}
$

### Wolfe-Powell准则

$
\begin{aligned}
&f(x^{k}+\overline{\alpha}\beta^{m}d^{k})-f(x^{k})\le \rho \overline{\alpha}\beta^{m}\nabla f(x^{k})^{T}d^{k}\\
&\nabla f(x^{k}+\overline{\alpha}\beta^{m}d^{k})^{T}d^{k}\ge \sigma \nabla f(x^{k})^{T}d^{k}\\
\end{aligned}
$

### 强Wolfe-Powell准则

$
\begin{aligned}
&步长\alpha _{k}>0\\
&f(x^{k}+\alpha _{k}d^{k})-f(x^{k})\le \rho \alpha _{k}\nabla f(x^{k})^{T}d^{k}\\
&|\nabla f(x^{k}+\alpha _{k}d^{k})^{T}d^{k}|\le -\sigma |\nabla f(x^{k})^{T}d^{k}|
\end{aligned}
$

### angle condition+wolfe-powell condition

$
\begin{aligned}
&1.f(x)连续可微且有下界,\nabla f包含在\\
&\{x|f(x)\leq f(x^{0})\}的开集O上一致连续\\
&d^{k}满足角条件:\theta_{k}\leq \frac{\pi }{2}-\mu\\
&其中\theta _{k}是d^{k}与-\nabla f(x^{k})的夹角\\
&\mu \in (0,\frac{\pi }{2})\\
&3.\alpha_{k}满足wolfe-powell条件\\
&则\exists k,s.t. \nabla f(x^{k})=0,或\nabla f(x^{k})\to 0\\
\end{aligned}
$


# Gradient-related condition + (exact, Armijo, Goldstein) line search

## 定义

称 $\{d^k\}$ 与 $\{x^k\}$ **梯度相关联**，若对任意收敛到非稳定点的子列 $\{x^{k_i}\}$，有：

1. $\{d^{k_i}\}$ 有界；
2. 
   $$
   \limsup_{i\to\infty}
   \nabla f(x^{k_i})^T d^{k_i}<0.
   $$

## 定理

若满足：

1. $f(x)$ 连续可微；
2. $\{d^k\}$ 和 $\{x^k\}$ 梯度相关联；
3. $\alpha_k$ 由（精确、Armijo、Goldstein）线搜索生成；

则

$$
\boxed{\{x^k\}\text{ 的每一聚点都是稳定点}.}
$$

## 证明



## 最速下降法

# 1. Gradient-related condition + (exact, Armijo, Goldstein) line search

## 定义

称 $\{d^k\}$ 与 $\{x^k\}$ **梯度相关联（gradient-related）**，若对任意收敛到非稳定点的子列 $\{x^{k_i}\}$，有：

1. $\{d^{k_i}\}$ 有界；

2. 
   $$
   \limsup_{i\to\infty}
   \nabla f(x^{k_i})^T d^{k_i}<0.
   $$

## 定理

若：

1. $f(x)$ 连续可微；

2. $\{d^k\}$ 和 $\{x^k\}$ 梯度相关联；

3. $\alpha_k$ 由（精确、Armijo、Goldstein）线搜索生成；

则

$$
\boxed{
\{x^k\}\text{ 的每一聚点都是稳定点}.
}
$$

## 证明

---

# 2. Zoutendijk condition

## 定理

若：

1. $f(x)$ 连续可微且下有界，$\nabla f(x)$ 在包含
   $$
   \{x\mid f(x)\le f(x^0)\}
   $$
   的开集 $O$ 上 Lipschitz 连续；

2. $d^k$ 是下降方向，即
   $$
   \nabla f(x^k)^T d^k<0;
   $$

3. $\alpha_k$ 由（Wolfe-Powell、强 Wolfe-Powell、Goldstein）线搜索生成；

则

$$
\boxed{
\sum_{k=1}^{\infty}
\|\nabla f(x^k)\|^2\cos^2\theta_k<\infty
}
$$

这称为 **Zoutendijk condition**。

## 证明

## 注

- Zoutendijk 条件意味着
  $$
  \|\nabla f(x^k)\|^2\cos^2\theta_k\to0.
  $$

- 若存在 $\delta>0$，使
  $$
  \cos\theta_k\ge\delta>0,\qquad \forall k,
  $$
  那么
  $$
  \|\nabla f(x^k)\|\to0.
  $$

- 欲证明某算法（例如 Newton、quasi-Newton）收敛，只需证明
  $$
  \cos\theta_k\ge\delta>0,\qquad \forall k.
  $$

---

# 3. Convergence: steepest descent + (exact, Armijo, Goldstein) line search

## 定理

若：

1. $f(x)$ 连续可微；

2. 搜索方向取为最速下降方向
   $$
   d^k=-\nabla f(x^k);
   $$

3. $\alpha_k$ 由（精确、Armijo、Goldstein）线搜索生成；

则

$$
\boxed{
\{x^k\}\text{ 或者有限步终止，或者它的聚点都是稳定点}.
}
$$

## 证明

**Hint:** show gradient-related condition.

---

# 4. Convergence: steepest descent + constant step size

## 定理

若：

1. $f(x)$ 连续可微且下有界，$\nabla f(x)$ Lipschitz 连续，Lipschitz 常数为 $L$；

2. 搜索方向取为
   $$
   d^k=-\nabla f(x^k);
   $$

3. 采用固定步长
   $$
   \alpha_k=\alpha\in\left(0,\frac1L\right);
   $$

则

$$
\boxed{
\{x^k\}\text{ 或者有限步终止，或者 }
\nabla f(x^k)\to0.
}
$$

## 证明